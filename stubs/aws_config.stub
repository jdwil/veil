stub aws-config 1.11.0
  # @generated veil-stub-gen 1
  # source crates.io
  # surface full
  # cargo_name aws-config
  # generated_at 2026-08-20T19:52:59Z
  # rustdoc_fingerprint e44e031bd03a34c7
  # Auto-inferred codegen policy from rustdoc (do not hand-edit; re-run veil stub-gen)
  fn defaults(version: BehaviorVersion) -> ConfigLoader
    lowers_to
      rust: "aws_config::defaults({{version}})"
  fn endpoint_url_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<Str>>
    lowers_to
      rust: "aws_config::endpoint_url_provider({{provider_config}}).await"
  fn endpoint_url_provider_with_origin(provider_config: ProviderConfig) -> BoxFuture<(Opt<Str>, Origin)>
    lowers_to
      rust: "aws_config::endpoint_url_provider_with_origin({{provider_config}}).await"
  fn from_env() -> ConfigLoader
    lowers_to
      rust: "aws_config::from_env({{args}})"
  fn ignore_configured_endpoint_urls_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<Bool>>
    lowers_to
      rust: "aws_config::ignore_configured_endpoint_urls_provider({{provider_config}}).await"
  fn load_defaults(version: BehaviorVersion) -> BoxFuture<SdkConfig>
    lowers_to
      rust: "aws_config::load_defaults({{version}}).await"
  fn load_from_env() -> BoxFuture<SdkConfig>
    lowers_to
      rust: "aws_config::load_from_env({{args}}).await"
  fn request_checksum_calculation_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<RequestChecksumCalculation>>
    lowers_to
      rust: "aws_config::request_checksum_calculation_provider({{provider_config}}).await"
  fn response_checksum_validation_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<ResponseChecksumValidation>>
    lowers_to
      rust: "aws_config::response_checksum_validation_provider({{provider_config}}).await"
  fn use_dual_stack_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<Bool>>
    lowers_to
      rust: "aws_config::use_dual_stack_provider({{provider_config}}).await"
  fn use_fips_provider(provider_config: ProviderConfig) -> BoxFuture<Opt<Bool>>
    lowers_to
      rust: "aws_config::use_fips_provider({{provider_config}}).await"

  struct ImdsCredentialsProvider
    path imds::credentials
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::ImdsCredentialsProvider::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct ImdsRegionProvider
    path imds::region
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::ImdsRegionProvider::builder({{args}})"
    fn region() -> BoxFuture<Opt<Region>>
      lowers_to
        rust: "{{self}}.region({{args}}).await"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct ProvideRegion
    path meta::region::future
    fn new(future: BoxFuture<Opt<Region>>) -> Self
      lowers_to
        rust: "aws_config::ProvideRegion::new({{future}})"
    fn ready(region: Opt<Region>) -> Self
      lowers_to
        rust: "aws_config::ProvideRegion::ready({{region}})"

  struct CredentialsProviderChain
    path meta::credentials::chain
    fn first_try(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "aws_config::CredentialsProviderChain::first_try({{name}}, {{provider}})"
    fn or_else(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.or_else({{name}}, {{provider}})"
    fn or_default_provider() -> BoxFuture<Self>
      lowers_to
        rust: "{{self}}.or_default_provider({{args}}).await"
    fn default_provider() -> BoxFuture<Self>
      lowers_to
        rust: "aws_config::CredentialsProviderChain::default_provider({{args}}).await"

  struct RegionProviderChain
    path meta::region
    fn region() -> BoxFuture<Opt<Region>>
      lowers_to
        rust: "{{self}}.region({{args}}).await"
    fn first_try(provider: Unknown) -> Self
      lowers_to
        rust: "aws_config::RegionProviderChain::first_try({{provider}})"
    fn or_else(fallback: Unknown) -> Self
      lowers_to
        rust: "{{self}}.or_else({{fallback}})"
    fn default_provider() -> Self
      lowers_to
        rust: "aws_config::RegionProviderChain::default_provider({{args}})"
    fn or_default_provider() -> Self
      lowers_to
        rust: "{{self}}.or_default_provider({{args}})"

  struct TokenProviderChain
    path meta::token
    fn first_try(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "aws_config::TokenProviderChain::first_try({{name}}, {{provider}})"
    fn or_else(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.or_else({{name}}, {{provider}})"
    fn or_default_provider() -> BoxFuture<Self>
      lowers_to
        rust: "{{self}}.or_default_provider({{args}}).await"
    fn default_provider() -> BoxFuture<Self>
      lowers_to
        rust: "aws_config::TokenProviderChain::default_provider({{args}}).await"

  struct ProviderAttempt
    path meta
    fn name() -> Str
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn error() -> E
      lowers_to
        rust: "{{self}}.error({{args}})"

  struct ProviderChainError
    path meta
    fn attempts() -> List<ProviderAttempt<E>>
      lowers_to
        rust: "{{self}}.attempts({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct ConfigLoader
    path loader
    fn behavior_version(behavior_version: BehaviorVersion) -> Self
      lowers_to
        rust: "{{self}}.behavior_version({{behavior_version}})"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn retry_config(retry_config: RetryConfig) -> Self
      lowers_to
        rust: "{{self}}.retry_config({{retry_config}})"
    fn timeout_config(timeout_config: TimeoutConfig) -> Self
      lowers_to
        rust: "{{self}}.timeout_config({{timeout_config}})"
    fn sleep_impl(sleep: Unknown) -> Self
      lowers_to
        rust: "{{self}}.sleep_impl({{sleep}})"
    fn time_source(time_source: Unknown) -> Self
      lowers_to
        rust: "{{self}}.time_source({{time_source}})"
    fn http_client(http_client: Unknown) -> Self
      lowers_to
        rust: "{{self}}.http_client({{http_client}})"
    fn protocol(protocol: Unknown) -> Self
      lowers_to
        rust: "{{self}}.protocol({{protocol}})"
    fn auth_scheme_preference(auth_scheme_preference: AuthSchemePreference) -> Self
      lowers_to
        rust: "{{self}}.auth_scheme_preference({{auth_scheme_preference}})"
    fn sigv4a_signing_region_set(sigv4a_signing_region_set: SigningRegionSet) -> Self
      lowers_to
        rust: "{{self}}.sigv4a_signing_region_set({{sigv4a_signing_region_set}})"
    fn identity_cache(identity_cache: Unknown) -> Self
      lowers_to
        rust: "{{self}}.identity_cache({{identity_cache}})"
    fn credentials_provider(credentials_provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.credentials_provider({{credentials_provider}})"
    fn no_credentials() -> Self
      lowers_to
        rust: "{{self}}.no_credentials({{args}})"
    fn test_credentials() -> Self
      lowers_to
        rust: "{{self}}.test_credentials({{args}})"
    fn empty_test_environment() -> Self
      lowers_to
        rust: "{{self}}.empty_test_environment({{args}})"
    fn token_provider(token_provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.token_provider({{token_provider}})"
    fn app_name(app_name: AppName) -> Self
      lowers_to
        rust: "{{self}}.app_name({{app_name}})"
    fn framework_metadata(framework_metadata: FrameworkMetadata) -> Self
      lowers_to
        rust: "{{self}}.framework_metadata({{framework_metadata}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn account_id_endpoint_mode(account_id_endpoint_mode: AccountIdEndpointMode) -> Self
      lowers_to
        rust: "{{self}}.account_id_endpoint_mode({{account_id_endpoint_mode}})"
    fn endpoint_url(endpoint_url: Str) -> Self
      lowers_to
        rust: "{{self}}.endpoint_url({{endpoint_url}})"
    fn use_fips(use_fips: Bool) -> Self
      lowers_to
        rust: "{{self}}.use_fips({{use_fips}})"
    fn use_dual_stack(use_dual_stack: Bool) -> Self
      lowers_to
        rust: "{{self}}.use_dual_stack({{use_dual_stack}})"
    fn disable_request_compression(disable_request_compression: Bool) -> Self
      lowers_to
        rust: "{{self}}.disable_request_compression({{disable_request_compression}})"
    fn request_min_compression_size_bytes(size: Int) -> Self
      lowers_to
        rust: "{{self}}.request_min_compression_size_bytes({{size}})"
    fn stalled_stream_protection(stalled_stream_protection_config: StalledStreamProtectionConfig) -> Self
      lowers_to
        rust: "{{self}}.stalled_stream_protection({{stalled_stream_protection_config}})"
    fn request_checksum_calculation(request_checksum_calculation: RequestChecksumCalculation) -> Self
      lowers_to
        rust: "{{self}}.request_checksum_calculation({{request_checksum_calculation}})"
    fn response_checksum_validation(response_checksum_validation: ResponseChecksumValidation) -> Self
      lowers_to
        rust: "{{self}}.response_checksum_validation({{response_checksum_validation}})"
    fn load() -> BoxFuture<SdkConfig>
      lowers_to
        rust: "{{self}}.load({{args}}).await"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct ProviderConfig
    path provider_config
    fn without_region() -> Self
      lowers_to
        rust: "aws_config::ProviderConfig::without_region({{args}})"
    fn empty() -> Self
      lowers_to
        rust: "aws_config::ProviderConfig::empty({{args}})"
    fn with_default_region() -> BoxFuture<Self>
      lowers_to
        rust: "aws_config::ProviderConfig::with_default_region({{args}}).await"
    fn with_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.with_region({{region}})"
    fn with_use_fips(use_fips: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.with_use_fips({{use_fips}})"
    fn with_use_dual_stack(use_dual_stack: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.with_use_dual_stack({{use_dual_stack}})"
    fn with_behavior_version(behavior_version: Opt<BehaviorVersion>) -> Self
      lowers_to
        rust: "{{self}}.with_behavior_version({{behavior_version}})"
    fn load_default_region() -> BoxFuture<Self>
      lowers_to
        rust: "{{self}}.load_default_region({{args}}).await"
    fn with_time_source(time_source: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_time_source({{time_source}})"
    fn with_http_client(http_client: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_http_client({{http_client}})"
    fn with_sleep_impl(sleep_impl: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_sleep_impl({{sleep_impl}})"
    fn with_retry_config(retry_config: RetryConfig) -> Self
      lowers_to
        rust: "{{self}}.with_retry_config({{retry_config}})"
    fn with_timeout_config(timeout_config: TimeoutConfig) -> Self
      lowers_to
        rust: "{{self}}.with_timeout_config({{timeout_config}})"

  struct ProfileFileTokenProvider
    path profile::token
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::ProfileFileTokenProvider::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct RetryConfigError
    path retry::error

  struct SsoCredentialsProvider
    path sso::credentials
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::SsoCredentialsProvider::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct SsoTokenProvider
    path sso::token
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::SsoTokenProvider::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct AssumeRoleProvider
    path sts::assume_role
    fn builder(role: Str) -> AssumeRoleProviderBuilder
      lowers_to
        rust: "aws_config::AssumeRoleProvider::builder({{role}})"

  struct AssumeRoleProviderBuilder
    path sts::assume_role
    fn new(role: Str) -> Self
      lowers_to
        rust: "aws_config::AssumeRoleProviderBuilder::new({{role}})"
    fn external_id(id: Str) -> Self
      lowers_to
        rust: "{{self}}.external_id({{id}})"
    fn session_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{name}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn session_length(length: Duration) -> Self
      lowers_to
        rust: "{{self}}.session_length({{length}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn tags(tags: Unknown) -> Self
      lowers_to
        rust: "{{self}}.tags({{tags}})"
    fn configure(conf: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{conf}})"
    fn build() -> BoxFuture<AssumeRoleProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn build_from_provider(provider: Unknown) -> BoxFuture<AssumeRoleProvider>
      lowers_to
        rust: "{{self}}.build_from_provider({{provider}}).await"

  struct WebIdentityTokenCredentialsProvider
    path web_identity_token
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::WebIdentityTokenCredentialsProvider::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct StaticConfiguration
    path web_identity_token

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct EnvironmentVariableCredentialsProvider
    path environment::credentials
    fn new() -> Self
      lowers_to
        rust: "aws_config::EnvironmentVariableCredentialsProvider::new({{args}})"

  struct ProfileFileCredentialsProvider
    path profile::credentials
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::ProfileFileCredentialsProvider::builder({{args}})"

  struct DefaultCredentialsChain
    path default_provider::credentials
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::DefaultCredentialsChain::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct Client
    path imds::client
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::Client::builder({{args}})"
    fn get(path: Str) -> BoxFuture<Res!<SensitiveString>>
      lowers_to
        rust: "{{self}}.get({{path}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct DefaultTokenChain
    path default_provider::token
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::DefaultTokenChain::builder({{args}})"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct EcsCredentialsProvider
    path ecs
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::EcsCredentialsProvider::builder({{args}})"
    fn credentials() -> BoxFuture<Res>
      lowers_to
        rust: "{{self}}.credentials({{args}}).await"

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct InvalidFullUriError
    path ecs

  struct FailedToLoadToken
    path imds::client::error
    fn is_dispatch_failure() -> Bool
      lowers_to
        rust: "{{self}}.is_dispatch_failure({{args}})"

  struct ErrorResponse
    path imds::client::error
    fn response() -> HttpResponse
      lowers_to
        rust: "{{self}}.response({{args}})"

  struct IoError
    path imds::client::error

  struct Unexpected
    path imds::client::error

  struct InvalidEndpointMode
    path imds::client::error

  struct BuildError
    path imds::client::error

  struct EnvironmentVariableRegionProvider
    path environment::region
    fn new() -> Self
      lowers_to
        rust: "aws_config::EnvironmentVariableRegionProvider::new({{args}})"

  struct ProfileFileRegionProvider
    path profile::region
    fn new() -> Self
      lowers_to
        rust: "aws_config::ProfileFileRegionProvider::new({{args}})"
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::ProfileFileRegionProvider::builder({{args}})"

  struct TokenError
    path imds::client::error

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct DefaultRegionChain
    path default_provider::region
    fn region() -> BoxFuture<Opt<Region>>
      lowers_to
        rust: "{{self}}.region({{args}}).await"
    fn builder() -> Builder
      lowers_to
        rust: "aws_config::DefaultRegionChain::builder({{args}})"

  struct SensitiveString
    path imds::client

  struct Builder
    path default_provider::region
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn profile(profile: Str) -> Self
      lowers_to
        rust: "{{self}}.profile({{profile}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn build() -> ImdsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn imds_client(imds_client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{imds_client}})"
    fn build() -> ImdsRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn with_custom_provider(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_provider({{name}}, {{provider}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileRegionProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{configuration}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn retry_config() -> BoxFuture<RetryConfig>
      lowers_to
        rust: "{{self}}.retry_config({{args}}).await"
    fn profile_name(profile_name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{profile_name}})"
    fn profile_files(profile_files: ProfileFiles) -> Self
      lowers_to
        rust: "{{self}}.profile_files({{profile_files}})"
    fn build() -> ProfileFileTokenProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn account_id(account_id: Str) -> Self
      lowers_to
        rust: "{{self}}.account_id({{account_id}})"
    fn set_account_id(account_id: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_account_id({{account_id}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn role_name(role_name: Str) -> Self
      lowers_to
        rust: "{{self}}.role_name({{role_name}})"
    fn set_role_name(role_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_role_name({{role_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn build() -> SsoCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn new() -> Self
      lowers_to
        rust: "aws_config::Builder::new({{args}})"
    fn configure(sdk_config: SdkConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{sdk_config}})"
    fn region(region: Region) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Region>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn session_name(session_name: Str) -> Self
      lowers_to
        rust: "{{self}}.session_name({{session_name}})"
    fn set_session_name(session_name: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_session_name({{session_name}})"
    fn start_url(start_url: Str) -> Self
      lowers_to
        rust: "{{self}}.start_url({{start_url}})"
    fn set_start_url(start_url: Opt<Str>) -> Self
      lowers_to
        rust: "{{self}}.set_start_url({{start_url}})"
    fn build() -> BoxFuture<SsoTokenProvider>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn app_name() -> BoxFuture<Opt<AppName>>
      lowers_to
        rust: "{{self}}.app_name({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn static_configuration(config: StaticConfiguration) -> Self
      lowers_to
        rust: "{{self}}.static_configuration({{config}})"
    fn policy(policy: Str) -> Self
      lowers_to
        rust: "{{self}}.policy({{policy}})"
    fn policy_arns(policy_arns: List<Str>) -> Self
      lowers_to
        rust: "{{self}}.policy_arns({{policy_arns}})"
    fn build() -> WebIdentityTokenCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn configure(_configuration: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{_configuration}})"
    fn timeout_config() -> BoxFuture<TimeoutConfig>
      lowers_to
        rust: "{{self}}.timeout_config({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn with_custom_credential_source(name: Cow<Str>, provider: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_custom_credential_source({{name}}, {{provider}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn imds_client(client: Client) -> Self
      lowers_to
        rust: "{{self}}.imds_client({{client}})"
    fn configure(config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{config}})"
    fn build() -> BoxFuture<DefaultCredentialsChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn region(region: Unknown) -> Self
      lowers_to
        rust: "{{self}}.region({{region}})"
    fn set_region(region: Opt<Unknown>) -> Self
      lowers_to
        rust: "{{self}}.set_region({{region}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> BoxFuture<DefaultTokenChain>
      lowers_to
        rust: "{{self}}.build({{args}}).await"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn dns(dns: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dns({{dns}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn build() -> EcsCredentialsProvider
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn max_attempts(max_attempts: Int) -> Self
      lowers_to
        rust: "{{self}}.max_attempts({{max_attempts}})"
    fn configure(provider_config: ProviderConfig) -> Self
      lowers_to
        rust: "{{self}}.configure({{provider_config}})"
    fn endpoint(endpoint: Str) -> Res!<Self>
      lowers_to
        rust: "{{self}}.endpoint({{endpoint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn endpoint_mode(mode: EndpointMode) -> Self
      lowers_to
        rust: "{{self}}.endpoint_mode({{mode}})"
    fn token_ttl(ttl: Duration) -> Self
      lowers_to
        rust: "{{self}}.token_ttl({{ttl}})"
    fn connect_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn operation_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_timeout({{timeout}})"
    fn operation_attempt_timeout(timeout: Duration) -> Self
      lowers_to
        rust: "{{self}}.operation_attempt_timeout({{timeout}})"
    fn retry_classifier(retry_classifier: SharedRetryClassifier) -> Self
      lowers_to
        rust: "{{self}}.retry_classifier({{retry_classifier}})"
    fn build() -> Client
      lowers_to
        rust: "{{self}}.build({{args}})"
    fn profile_name(name: Str) -> Self
      lowers_to
        rust: "{{self}}.profile_name({{name}})"
    fn build() -> DefaultRegionChain
      lowers_to
        rust: "{{self}}.build({{args}})"

  struct CredentialProcessProvider
    path credential_process
    fn new(command: Str) -> Self
      lowers_to
        rust: "aws_config::CredentialProcessProvider::new({{command}})"

  struct ImdsResponseRetryClassifier
    path imds::client
    fn with_retry_connect_timeouts(retry_connect_timeouts: Bool) -> Self
      lowers_to
        rust: "{{self}}.with_retry_connect_timeouts({{retry_connect_timeouts}})"

  enum ProfileFileError
    InvalidProfile(ProfileFileLoadError)
    NoProfilesDefined
    ProfileDidNotContainCredentials
    CredentialLoop
    MissingCredentialSource
    InvalidCredentialSource
    MissingProfile
    UnknownProvider
    FeatureNotEnabled
    MissingSsoSession
    InvalidSsoConfig
    TokenProviderConfig

  enum ImdsError
    FailedToLoadToken(FailedToLoadToken)
    ErrorResponse(ErrorResponse)
    IoError(IoError)
    Unexpected(Unexpected)

  enum EndpointMode
    IpV4
    IpV6

  trait ProvideRegion
    fn region() -> ProvideRegion
      lowers_to
        rust: "{{self}}.region({{args}})"
