stub gix 0.86.0
  # @generated veil-stub-gen 1
  # source crates.io
  # surface full
  # cargo_name gix
  # generated_at 2026-08-20T19:51:46Z
  # rustdoc_fingerprint 582cbc7f4a4dc8b0
  # features tree-editor
  # Auto-inferred codegen policy from rustdoc (do not hand-edit; re-run veil stub-gen)
  cargo_features tree-editor
  fn agent() -> Str
    lowers_to
      rust: "gix::agent({{args}})"
  fn args_os() -> Unknown
    lowers_to
      rust: "gix::args_os({{args}})"
  fn args_os_opt(precompose_unicode: Bool) -> Unknown
    lowers_to
      rust: "gix::args_os_opt({{precompose_unicode}})"
  fn init_bare(directory: Path) -> Res!<Repository>
    lowers_to
      rust: "gix::init_bare({{directory}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn init_handler(grace_count: Int, interrupt: Unknown) -> Res!<Deregister>
    lowers_to
      rust: "gix::init_handler({{grace_count}}, {{interrupt}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn into(directory: PathBuf, kind: Kind, _: Options) -> Res!<Path>
    lowers_to
      rust: "gix::into({{directory}}, {{kind}}, {{_}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn is_triggered() -> Bool
    lowers_to
      rust: "gix::is_triggered({{args}})"
  fn is_trusted(meta: Metadata) -> Bool
    lowers_to
      rust: "gix::is_trusted({{meta}})"
  fn new_rewrites(config: File, lenient: Bool) -> Res!<(Opt<Rewrites>, Bool)>
    lowers_to
      rust: "gix::new_rewrites({{config}}, {{lenient}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn os_str_to_bstring(input: OsStr) -> Opt<BString>
    lowers_to
      rust: "gix::os_str_to_bstring({{input}})"
  fn prepare_clone(url: Url, path: Path) -> Res!<PrepareFetch>
    lowers_to
      rust: "gix::prepare_clone({{url}}, {{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn prepare_clone_bare(url: Url, path: Path) -> Res!<PrepareFetch>
    lowers_to
      rust: "gix::prepare_clone_bare({{url}}, {{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn reset()
    lowers_to
      rust: "gix::reset({{args}})"
  fn resource_cache(repo: Repository, mode: Mode, attr_stack: Stack, roots: WorktreeRoots) -> Res!<Platform>
    lowers_to
      rust: "gix::resource_cache({{repo}}, {{mode}}, {{attr_stack}}, {{roots}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
  fn trigger()
    lowers_to
      rust: "gix::trigger({{args}})"
  fn validated(name: BString) -> Res!<BString>
    lowers_to
      rust: "gix::validated({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Read
    path interrupt
    fn new(read: R) -> Self
      lowers_to
        rust: "gix::Read::new({{read}})"
    fn into_inner() -> R
      lowers_to
        rust: "{{self}}.into_inner({{args}})"

  struct Pathspec
    path types
    fn new(repo: Repository, empty_patterns_match_prefix: Bool, patterns: Unknown, inherit_ignore_case: Bool, make_attributes: Unknown) -> Res!<Self>
      lowers_to
        rust: "gix::Pathspec::new({{repo}}, {{empty_patterns_match_prefix}}, {{patterns}}, {{inherit_ignore_case}}, {{make_attributes}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_parts() -> (Search, Opt<AttributeStack>)
      lowers_to
        rust: "{{self}}.into_parts({{args}})"
    fn detach() -> Res!<PathspecDetached>
      lowers_to
        rust: "{{self}}.detach({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn attributes() -> Opt<Stack>
      lowers_to
        rust: "{{self}}.attributes({{args}})"
    fn search() -> Search
      lowers_to
        rust: "{{self}}.search({{args}})"
    fn pattern_matching_relative_path(relative_path: BStr, is_dir: Opt<Bool>) -> Opt<Match>
      lowers_to
        rust: "{{self}}.pattern_matching_relative_path({{relative_path}}, {{is_dir}})"
    fn is_included(relative_path: BStr, is_dir: Opt<Bool>) -> Bool
      lowers_to
        rust: "{{self}}.is_included({{relative_path}}, {{is_dir}})"
    fn index_entries_with_paths(index: State) -> Opt<Unknown>
      lowers_to
        rust: "{{self}}.index_entries_with_paths({{index}})"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct BuiltinSubmoduleStatus
    path status::index_worktree
    fn new(repo: ThreadSafeRepository, mode: Submodule) -> Res!<Self>
      lowers_to
        rust: "gix::BuiltinSubmoduleStatus::new({{repo}}, {{mode}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Outcome
    path dirwalk
    fn has_changes() -> Bool
      lowers_to
        rust: "{{self}}.has_changes({{args}})"
    fn write_changes() -> Opt<Res!<()>>
      lowers_to
        rust: "{{self}}.write_changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Iter
    path dirwalk
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn peeled() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peeled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(inner: I, make_err: EFN) -> Self
      lowers_to
        rust: "gix::Iter::new({{inner}}, {{make_err}})"
    fn into_inner() -> I
      lowers_to
        rust: "{{self}}.into_inner({{args}})"
    fn inner() -> I
      lowers_to
        rust: "{{self}}.inner({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"

  struct Iter
    path dirwalk
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn peeled() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peeled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(inner: I, make_err: EFN) -> Self
      lowers_to
        rust: "gix::Iter::new({{inner}}, {{make_err}})"
    fn into_inner() -> I
      lowers_to
        rust: "{{self}}.into_inner({{args}})"
    fn inner() -> I
      lowers_to
        rust: "{{self}}.inner({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"

  struct Submodule
    path types
    fn status(ignore: Ignore, check_dirty: Bool) -> Res!<Status>
      lowers_to
        rust: "{{self}}.status({{ignore}}, {{check_dirty}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn status_opts(ignore: Ignore, check_dirty: Bool, adjust_options: Unknown) -> Res!<Status>
      lowers_to
        rust: "{{self}}.status_opts({{ignore}}, {{check_dirty}}, {{adjust_options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn name() -> BStr
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn validated_name() -> Res!<BStr>
      lowers_to
        rust: "{{self}}.validated_name({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn path() -> Res!<BString>
      lowers_to
        rust: "{{self}}.path({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn url() -> Res!<Url>
      lowers_to
        rust: "{{self}}.url({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn update() -> Res!<Opt<Update>>
      lowers_to
        rust: "{{self}}.update({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn branch() -> Res!<Opt<Branch>>
      lowers_to
        rust: "{{self}}.branch({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn fetch_recurse() -> Res!<Opt<FetchRecurse>>
      lowers_to
        rust: "{{self}}.fetch_recurse({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn ignore() -> Res!<Opt<Ignore>>
      lowers_to
        rust: "{{self}}.ignore({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn shallow() -> Res!<Opt<Bool>>
      lowers_to
        rust: "{{self}}.shallow({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn is_active() -> Res!<Bool>
      lowers_to
        rust: "{{self}}.is_active({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_id() -> Res!<Opt<ObjectId>>
      lowers_to
        rust: "{{self}}.index_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_id() -> Res!<Opt<ObjectId>>
      lowers_to
        rust: "{{self}}.head_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.git_dir({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn work_dir() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.work_dir({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir_try_old_form() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.git_dir_try_old_form({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn state() -> Res!<State>
      lowers_to
        rust: "{{self}}.state({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open() -> Res!<Opt<Repository>>
      lowers_to
        rust: "{{self}}.open({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Proxy
    path worktree
    fn base() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.base({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir() -> Path
      lowers_to
        rust: "{{self}}.git_dir({{args}})"
    fn id() -> BStr
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn is_locked() -> Bool
      lowers_to
        rust: "{{self}}.is_locked({{args}})"
    fn lock_reason() -> Opt<BString>
      lowers_to
        rust: "{{self}}.lock_reason({{args}})"
    fn into_repo_with_possibly_inaccessible_worktree() -> Res!<Repository>
      lowers_to
        rust: "{{self}}.into_repo_with_possibly_inaccessible_worktree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_repo() -> Res!<Repository>
      lowers_to
        rust: "{{self}}.into_repo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Outcome
    path dirwalk
    fn has_changes() -> Bool
      lowers_to
        rust: "{{self}}.has_changes({{args}})"
    fn write_changes() -> Opt<Res!<()>>
      lowers_to
        rust: "{{self}}.write_changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct PrepareFetch
    path clone
    fn configure_remote(f: Unknown) -> Self
      lowers_to
        rust: "{{self}}.configure_remote({{f}})"
    fn with_remote_name(name: BString) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_remote_name({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_shallow(shallow: Shallow) -> Self
      lowers_to
        rust: "{{self}}.with_shallow({{shallow}})"
    fn with_in_memory_config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_in_memory_config_overrides({{values}})"
    fn with_ref_name(name: Opt<Name>) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_ref_name({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn persist() -> Repository
      lowers_to
        rust: "{{self}}.persist({{args}})"
    fn new(url: Url, path: Path, kind: Kind, create_opts: Options, open_opts: Options) -> Res!<Self>
      lowers_to
        rust: "gix::PrepareFetch::new({{url}}, {{path}}, {{kind}}, {{create_opts}}, {{open_opts}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct PrepareCheckout
    path clone
    fn main_worktree(progress: P, should_interrupt: AtomicBool) -> Res!<(Repository, Outcome)>
      lowers_to
        rust: "{{self}}.main_worktree({{progress}}, {{should_interrupt}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn repo() -> Repository
      lowers_to
        rust: "{{self}}.repo({{args}})"
    fn persist() -> Repository
      lowers_to
        rust: "{{self}}.persist({{args}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct PathspecDetached
    path types
    fn pattern_matching_relative_path(relative_path: BStr, is_dir: Opt<Bool>) -> Opt<Match>
      lowers_to
        rust: "{{self}}.pattern_matching_relative_path({{relative_path}}, {{is_dir}})"
    fn is_included(relative_path: BStr, is_dir: Opt<Bool>) -> Bool
      lowers_to
        rust: "{{self}}.is_included({{relative_path}}, {{is_dir}})"

  struct Repository
    path types
    fn attributes(index: State, attributes_source: Source, ignore_source: Source, exclude_overrides: Opt<Search>) -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.attributes({{index}}, {{attributes_source}}, {{ignore_source}}, {{exclude_overrides}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn attributes_only(index: State, attributes_source: Source) -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.attributes_only({{index}}, {{attributes_source}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn excludes(index: State, overrides: Opt<Search>, source: Source) -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.excludes({{index}}, {{overrides}}, {{source}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn blame_file(file_path: BStr, suspect: ObjectId, options: Options) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.blame_file({{file_path}}, {{suspect}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn object_cache_size(bytes: Opt<Int>)
      lowers_to
        rust: "{{self}}.object_cache_size({{bytes}})"
    fn object_cache_size_if_unset(bytes: Int)
      lowers_to
        rust: "{{self}}.object_cache_size_if_unset({{bytes}})"
    fn compute_object_cache_size_for_tree_diffs(index: State) -> Int
      lowers_to
        rust: "{{self}}.compute_object_cache_size_for_tree_diffs({{index}})"
    fn with_object_memory() -> Self
      lowers_to
        rust: "{{self}}.with_object_memory({{args}})"
    fn checkout_options(attributes_source: Source) -> Res!<Options>
      lowers_to
        rust: "{{self}}.checkout_options({{attributes_source}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn branch_names() -> Set<Str>
      lowers_to
        rust: "{{self}}.branch_names({{args}})"
    fn branch_remote_ref_name(name: FullNameRef, direction: Direction) -> Opt<Res!<FullName>>
      lowers_to
        rust: "{{self}}.branch_remote_ref_name({{name}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn branch_remote_tracking_ref_name(name: FullNameRef, direction: Direction) -> Opt<Res!<FullName>>
      lowers_to
        rust: "{{self}}.branch_remote_tracking_ref_name({{name}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn upstream_branch_and_remote_for_tracking_branch(tracking_branch: FullNameRef) -> Res!<Opt<(FullName, Remote)>>
      lowers_to
        rust: "{{self}}.upstream_branch_and_remote_for_tracking_branch({{tracking_branch}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn branch_remote_name(short_branch_name: BStr, direction: Direction) -> Opt<Name>
      lowers_to
        rust: "{{self}}.branch_remote_name({{short_branch_name}}, {{direction}})"
    fn branch_remote(short_branch_name: BStr, direction: Direction) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.branch_remote({{short_branch_name}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_names() -> Names
      lowers_to
        rust: "{{self}}.remote_names({{args}})"
    fn remote_default_name(direction: Direction) -> Opt<BString>
      lowers_to
        rust: "{{self}}.remote_default_name({{direction}})"
    fn loose_compression() -> Compression
      lowers_to
        rust: "{{self}}.loose_compression({{args}})"
    fn pack_compression() -> Res!<Compression>
      lowers_to
        rust: "{{self}}.pack_compression({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn config_snapshot() -> Snapshot
      lowers_to
        rust: "{{self}}.config_snapshot({{args}})"
    fn config_snapshot_mut() -> SnapshotMut
      lowers_to
        rust: "{{self}}.config_snapshot_mut({{args}})"
    fn filesystem_options() -> Res!<Capabilities>
      lowers_to
        rust: "{{self}}.filesystem_options({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn stat_options() -> Res!<Options>
      lowers_to
        rust: "{{self}}.stat_options({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open_options() -> Options
      lowers_to
        rust: "{{self}}.open_options({{args}})"
    fn big_file_threshold() -> Res!<Int>
      lowers_to
        rust: "{{self}}.big_file_threshold({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn ignore_pattern_parser() -> Res!<Ignore>
      lowers_to
        rust: "{{self}}.ignore_pattern_parser({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn command_context() -> Res!<Context>
      lowers_to
        rust: "{{self}}.command_context({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn object_hash() -> Kind
      lowers_to
        rust: "{{self}}.object_hash({{args}})"
    fn diff_algorithm() -> Res!<Algorithm>
      lowers_to
        rust: "{{self}}.diff_algorithm({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn diff_resource_cache(mode: Mode, worktree_roots: WorktreeRoots) -> Res!<Platform>
      lowers_to
        rust: "{{self}}.diff_resource_cache({{mode}}, {{worktree_roots}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn diff_tree_to_tree(old_tree: Opt<Tree>, new_tree: Opt<Tree>, options: Opt<Options>) -> Res!<List<ChangeDetached>>
      lowers_to
        rust: "{{self}}.diff_tree_to_tree({{old_tree}}, {{new_tree}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn diff_resource_cache_for_tree_diff() -> Res!<Platform>
      lowers_to
        rust: "{{self}}.diff_resource_cache_for_tree_diff({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options() -> Res!<Options>
      lowers_to
        rust: "{{self}}.dirwalk_options({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk(index: State, patterns: Unknown, should_interrupt: AtomicBool, options: Options, delegate: Unknown) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.dirwalk({{index}}, {{patterns}}, {{should_interrupt}}, {{options}}, {{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_iter(index: IndexPersistedOrInMemory, patterns: Unknown, should_interrupt: OwnedOrStaticAtomicBool, options: Options) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.dirwalk_iter({{index}}, {{patterns}}, {{should_interrupt}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn filter_pipeline(tree_if_bare: Opt<ObjectId>) -> Res!<(Pipeline, IndexPersistedOrInMemory)>
      lowers_to
        rust: "{{self}}.filter_pipeline({{tree_if_bare}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn empty_reusable_buffer() -> Buffer
      lowers_to
        rust: "{{self}}.empty_reusable_buffer({{args}})"
    fn set_freelist(list: Opt<List<List<Int>>>) -> Opt<List<List<Int>>>
      lowers_to
        rust: "{{self}}.set_freelist({{list}})"
    fn without_freelist() -> Self
      lowers_to
        rust: "{{self}}.without_freelist({{args}})"
    fn revision_graph(cache: Opt<Graph>) -> Graph<T>
      lowers_to
        rust: "{{self}}.revision_graph({{cache}})"
    fn commit_graph() -> Res!<Graph>
      lowers_to
        rust: "{{self}}.commit_graph({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn commit_graph_if_enabled() -> Res!<Opt<Graph>>
      lowers_to
        rust: "{{self}}.commit_graph_if_enabled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn committer() -> Opt<Res!<SignatureRef>>
      lowers_to
        rust: "{{self}}.committer({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn committer_or_set_generic_fallback() -> Res!<SignatureRef>
      lowers_to
        rust: "{{self}}.committer_or_set_generic_fallback({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn author() -> Opt<Res!<SignatureRef>>
      lowers_to
        rust: "{{self}}.author({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open_index() -> Res!<File>
      lowers_to
        rust: "{{self}}.open_index({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index() -> Res!<Index>
      lowers_to
        rust: "{{self}}.index({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_or_empty() -> Res!<Index>
      lowers_to
        rust: "{{self}}.index_or_empty({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_index() -> Res!<Opt<Index>>
      lowers_to
        rust: "{{self}}.try_index({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_or_load_from_head() -> Res!<IndexPersistedOrInMemory>
      lowers_to
        rust: "{{self}}.index_or_load_from_head({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_or_load_from_head_or_empty() -> Res!<IndexPersistedOrInMemory>
      lowers_to
        rust: "{{self}}.index_or_load_from_head_or_empty({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_from_tree(tree: oid) -> Res!<File>
      lowers_to
        rust: "{{self}}.index_from_tree({{tree}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_sync() -> ThreadSafeRepository
      lowers_to
        rust: "{{self}}.into_sync({{args}})"
    fn reload() -> Res!<Self>
      lowers_to
        rust: "{{self}}.reload({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir() -> Path
      lowers_to
        rust: "{{self}}.git_dir({{args}})"
    fn git_dir_trust() -> Trust
      lowers_to
        rust: "{{self}}.git_dir_trust({{args}})"
    fn current_dir() -> Path
      lowers_to
        rust: "{{self}}.current_dir({{args}})"
    fn common_dir() -> Path
      lowers_to
        rust: "{{self}}.common_dir({{args}})"
    fn index_path() -> PathBuf
      lowers_to
        rust: "{{self}}.index_path({{args}})"
    fn modules_path() -> Opt<PathBuf>
      lowers_to
        rust: "{{self}}.modules_path({{args}})"
    fn path() -> Path
      lowers_to
        rust: "{{self}}.path({{args}})"
    fn work_dir() -> Opt<Path>
      lowers_to
        rust: "{{self}}.work_dir({{args}})"
    fn set_workdir(workdir: Opt<PathBuf>) -> Res!<Opt<PathBuf>>
      lowers_to
        rust: "{{self}}.set_workdir({{workdir}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn workdir() -> Opt<Path>
      lowers_to
        rust: "{{self}}.workdir({{args}})"
    fn workdir_path(rela_path: BStr) -> Opt<PathBuf>
      lowers_to
        rust: "{{self}}.workdir_path({{rela_path}})"
    fn normalize_path(path: Unknown) -> Res!<Cow<BStr>>
      lowers_to
        rust: "{{self}}.normalize_path({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn install_dir() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.install_dir({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefix() -> Res!<Opt<Path>>
      lowers_to
        rust: "{{self}}.prefix({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn kind() -> Kind
      lowers_to
        rust: "{{self}}.kind({{args}})"
    fn is_pristine() -> Opt<Bool>
      lowers_to
        rust: "{{self}}.is_pristine({{args}})"
    fn open_mailmap() -> Snapshot
      lowers_to
        rust: "{{self}}.open_mailmap({{args}})"
    fn open_mailmap_into(target: Snapshot) -> Res!<()>
      lowers_to
        rust: "{{self}}.open_mailmap_into({{target}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn edit_tree(id: ObjectId) -> Res!<Editor>
      lowers_to
        rust: "{{self}}.edit_tree({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_object(id: ObjectId) -> Res!<Object>
      lowers_to
        rust: "{{self}}.find_object({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_commit(id: ObjectId) -> Res!<Commit>
      lowers_to
        rust: "{{self}}.find_commit({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_tree(id: ObjectId) -> Res!<Tree>
      lowers_to
        rust: "{{self}}.find_tree({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_tag(id: ObjectId) -> Res!<Tag>
      lowers_to
        rust: "{{self}}.find_tag({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_blob(id: ObjectId) -> Res!<Blob>
      lowers_to
        rust: "{{self}}.find_blob({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_header(id: ObjectId) -> Res!<Header>
      lowers_to
        rust: "{{self}}.find_header({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn has_object(id: oid) -> Bool
      lowers_to
        rust: "{{self}}.has_object({{id}})"
    fn try_find_header(id: ObjectId) -> Res!<Opt<Header>>
      lowers_to
        rust: "{{self}}.try_find_header({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_find_object(id: ObjectId) -> Res!<Opt<Object>>
      lowers_to
        rust: "{{self}}.try_find_object({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn write_object(object: Unknown) -> Res!<Id>
      lowers_to
        rust: "{{self}}.write_object({{object}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn write_blob(bytes: List<Int>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.write_blob({{bytes}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn write_blob_stream(bytes: Unknown) -> Res!<Id>
      lowers_to
        rust: "{{self}}.write_blob_stream({{bytes}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tag(name: Str, target: oid, target_kind: Kind, tagger: Opt<SignatureRef>, message: Str, constraint: PreviousValue) -> Res!<Reference>
      lowers_to
        rust: "{{self}}.tag({{name}}, {{target}}, {{target_kind}}, {{tagger}}, {{message}}, {{constraint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn commit_as(committer: SignatureRef, author: SignatureRef, reference: Name, message: Str, tree: ObjectId, parents: Unknown) -> Res!<Id>
      lowers_to
        rust: "{{self}}.commit_as({{committer}}, {{author}}, {{reference}}, {{message}}, {{tree}}, {{parents}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn commit(reference: Name, message: Str, tree: ObjectId, parents: Unknown) -> Res!<Id>
      lowers_to
        rust: "{{self}}.commit({{reference}}, {{message}}, {{tree}}, {{parents}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_commit(message: Str, tree: ObjectId, parents: Unknown) -> Res!<Commit>
      lowers_to
        rust: "{{self}}.new_commit({{message}}, {{tree}}, {{parents}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_commit_as(committer: SignatureRef, author: SignatureRef, message: Str, tree: ObjectId, parents: Unknown) -> Res!<Commit>
      lowers_to
        rust: "{{self}}.new_commit_as({{committer}}, {{author}}, {{message}}, {{tree}}, {{parents}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn empty_tree() -> Tree
      lowers_to
        rust: "{{self}}.empty_tree({{args}})"
    fn empty_blob() -> Blob
      lowers_to
        rust: "{{self}}.empty_blob({{args}})"
    fn pathspec(empty_patterns_match_prefix: Bool, patterns: Unknown, inherit_ignore_case: Bool, index: State, attributes_source: Source) -> Res!<Pathspec>
      lowers_to
        rust: "{{self}}.pathspec({{empty_patterns_match_prefix}}, {{patterns}}, {{inherit_ignore_case}}, {{index}}, {{attributes_source}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pathspec_defaults() -> Res!<Defaults>
      lowers_to
        rust: "{{self}}.pathspec_defaults({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pathspec_defaults_inherit_ignore_case(inherit_ignore_case: Bool) -> Res!<Defaults>
      lowers_to
        rust: "{{self}}.pathspec_defaults_inherit_ignore_case({{inherit_ignore_case}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tag_reference(name: Str, target: ObjectId, constraint: PreviousValue) -> Res!<Reference>
      lowers_to
        rust: "{{self}}.tag_reference({{name}}, {{target}}, {{constraint}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn namespace() -> Opt<Namespace>
      lowers_to
        rust: "{{self}}.namespace({{args}})"
    fn clear_namespace() -> Opt<Namespace>
      lowers_to
        rust: "{{self}}.clear_namespace({{args}})"
    fn set_namespace(namespace: Name) -> Res!<Opt<Namespace>>
      lowers_to
        rust: "{{self}}.set_namespace({{namespace}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn reference(name: Name, target: ObjectId, constraint: PreviousValue, log_message: BString) -> Res!<Reference>
      lowers_to
        rust: "{{self}}.reference({{name}}, {{target}}, {{constraint}}, {{log_message}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn edit_reference(edit: RefEdit) -> Res!<List<RefEdit>>
      lowers_to
        rust: "{{self}}.edit_reference({{edit}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn edit_references(edits: Unknown) -> Res!<List<RefEdit>>
      lowers_to
        rust: "{{self}}.edit_references({{edits}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn edit_references_as(edits: Unknown, committer: Opt<SignatureRef>) -> Res!<List<RefEdit>>
      lowers_to
        rust: "{{self}}.edit_references_as({{edits}}, {{committer}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head() -> Res!<Head>
      lowers_to
        rust: "{{self}}.head({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.head_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_name() -> Res!<Opt<FullName>>
      lowers_to
        rust: "{{self}}.head_name({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_ref() -> Res!<Opt<Reference>>
      lowers_to
        rust: "{{self}}.head_ref({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_commit() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.head_commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_tree_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.head_tree_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_tree_id_or_empty() -> Res!<Id>
      lowers_to
        rust: "{{self}}.head_tree_id_or_empty({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.head_tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_reference(name: Name) -> Res!<Reference>
      lowers_to
        rust: "{{self}}.find_reference({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn references() -> Res!<Platform>
      lowers_to
        rust: "{{self}}.references({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_find_reference(name: Name) -> Res!<Opt<Reference>>
      lowers_to
        rust: "{{self}}.try_find_reference({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_at(url: Url) -> Res!<Remote>
      lowers_to
        rust: "{{self}}.remote_at({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_at_without_url_rewrite(url: Url) -> Res!<Remote>
      lowers_to
        rust: "{{self}}.remote_at_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_remote(name_or_url: Unknown) -> Res!<Remote>
      lowers_to
        rust: "{{self}}.find_remote({{name_or_url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_default_remote(direction: Direction) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.find_default_remote({{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_find_remote(name_or_url: BStr) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.try_find_remote({{name_or_url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_fetch_remote(name_or_url: Opt<BStr>) -> Res!<Remote>
      lowers_to
        rust: "{{self}}.find_fetch_remote({{name_or_url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_find_remote_without_url_rewrite(name_or_url: BStr) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.try_find_remote_without_url_rewrite({{name_or_url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn rev_parse(spec: BStr) -> Res!<Spec>
      lowers_to
        rust: "{{self}}.rev_parse({{spec}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn rev_parse_single(spec: BStr) -> Res!<Id>
      lowers_to
        rust: "{{self}}.rev_parse_single({{spec}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_base(one: ObjectId, two: ObjectId) -> Res!<Id>
      lowers_to
        rust: "{{self}}.merge_base({{one}}, {{two}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_base_with_graph(one: ObjectId, two: ObjectId, graph: Graph<Commit<Flags>>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.merge_base_with_graph({{one}}, {{two}}, {{graph}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_bases_many_with_graph(one: ObjectId, others: List<ObjectId>, graph: Graph<Commit<Flags>>) -> Res!<List<Id>>
      lowers_to
        rust: "{{self}}.merge_bases_many_with_graph({{one}}, {{others}}, {{graph}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_bases_many(one: ObjectId, others: List<ObjectId>) -> Res!<List<Id>>
      lowers_to
        rust: "{{self}}.merge_bases_many({{one}}, {{others}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_base_octopus_with_graph(commits: Unknown, graph: Graph<Commit<Flags>>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.merge_base_octopus_with_graph({{commits}}, {{graph}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn merge_base_octopus(commits: Unknown) -> Res!<Id>
      lowers_to
        rust: "{{self}}.merge_base_octopus({{commits}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn rev_walk(tips: Unknown) -> Platform
      lowers_to
        rust: "{{self}}.rev_walk({{tips}})"
    fn is_shallow() -> Bool
      lowers_to
        rust: "{{self}}.is_shallow({{args}})"
    fn shallow_commits() -> Res!<Opt<Commits>>
      lowers_to
        rust: "{{self}}.shallow_commits({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn shallow_file() -> PathBuf
      lowers_to
        rust: "{{self}}.shallow_file({{args}})"
    fn state() -> Opt<InProgress>
      lowers_to
        rust: "{{self}}.state({{args}})"
    fn open_modules_file() -> Res!<Opt<File>>
      lowers_to
        rust: "{{self}}.open_modules_file({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn modules() -> Res!<Opt<ModulesSnapshot>>
      lowers_to
        rust: "{{self}}.modules({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn submodules() -> Res!<Opt<Unknown>>
      lowers_to
        rust: "{{self}}.submodules({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn worktrees() -> Res!<List<Proxy>>
      lowers_to
        rust: "{{self}}.worktrees({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn worktree_proxy_by_id(id: BStr) -> Opt<Proxy>
      lowers_to
        rust: "{{self}}.worktree_proxy_by_id({{id}})"
    fn main_repo() -> Res!<Repository>
      lowers_to
        rust: "{{self}}.main_repo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn worktree() -> Opt<Worktree>
      lowers_to
        rust: "{{self}}.worktree({{args}})"
    fn is_bare() -> Bool
      lowers_to
        rust: "{{self}}.is_bare({{args}})"
    fn worktree_stream(id: ObjectId) -> Res!<(Stream, File)>
      lowers_to
        rust: "{{self}}.worktree_stream({{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn worktree_archive(stream: Stream, out: Unknown, blobs: Unknown, should_interrupt: AtomicBool, options: Options) -> Res!<()>
      lowers_to
        rust: "{{self}}.worktree_archive({{stream}}, {{out}}, {{blobs}}, {{should_interrupt}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn is_dirty() -> Res!<Bool>
      lowers_to
        rust: "{{self}}.is_dirty({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_worktree_status(index: State, patterns: Unknown, delegate: Unknown, compare: Unknown, submodule: Unknown, progress: Unknown, should_interrupt: AtomicBool, options: Options) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.index_worktree_status({{index}}, {{patterns}}, {{delegate}}, {{compare}}, {{submodule}}, {{progress}}, {{should_interrupt}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tree_index_status(tree_id: oid, worktree_index: State, pathspec: Opt<Pathspec>, renames: TrackRenames, cb: Unknown) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.tree_index_status({{tree_id}}, {{worktree_index}}, {{pathspec}}, {{renames}}, {{cb}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn status(progress: P) -> Res!<Platform<P>>
      lowers_to
        rust: "{{self}}.status({{progress}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Id
    path types
    fn object() -> Res!<Object>
      lowers_to
        rust: "{{self}}.object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn header() -> Res!<Header>
      lowers_to
        rust: "{{self}}.header({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_object() -> Res!<Opt<Object>>
      lowers_to
        rust: "{{self}}.try_object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_header() -> Res!<Opt<Header>>
      lowers_to
        rust: "{{self}}.try_header({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn shorten() -> Res!<Prefix>
      lowers_to
        rust: "{{self}}.shorten({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn shorten_or_id() -> Prefix
      lowers_to
        rust: "{{self}}.shorten_or_id({{args}})"
    fn detach() -> ObjectId
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn ancestors() -> Platform
      lowers_to
        rust: "{{self}}.ancestors({{args}})"

  struct Status
    path config::tree::sections
    fn is_dirty() -> Opt<Bool>
      lowers_to
        rust: "{{self}}.is_dirty({{args}})"

  struct State
    path submodule

  struct Reference
    path types
    fn remote_name(direction: Direction) -> Opt<Name>
      lowers_to
        rust: "{{self}}.remote_name({{direction}})"
    fn remote(direction: Direction) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.remote({{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_ref_name(direction: Direction) -> Opt<Res!<FullName>>
      lowers_to
        rust: "{{self}}.remote_ref_name({{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_tracking_ref_name(direction: Direction) -> Opt<Res!<FullName>>
      lowers_to
        rust: "{{self}}.remote_tracking_ref_name({{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn log_iter() -> Platform
      lowers_to
        rust: "{{self}}.log_iter({{args}})"
    fn log_exists() -> Bool
      lowers_to
        rust: "{{self}}.log_exists({{args}})"
    fn set_target_id(id: ObjectId, reflog_message: BString) -> Res!<()>
      lowers_to
        rust: "{{self}}.set_target_id({{id}}, {{reflog_message}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn delete() -> Res!<()>
      lowers_to
        rust: "{{self}}.delete({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_id() -> Opt<Id>
      lowers_to
        rust: "{{self}}.try_id({{args}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn target() -> TargetRef
      lowers_to
        rust: "{{self}}.target({{args}})"
    fn name() -> FullNameRef
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn detach() -> Reference
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn peel_to_id_in_place() -> Res!<Id>
      lowers_to
        rust: "{{self}}.peel_to_id_in_place({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.peel_to_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_id_in_place_packed(packed: Opt<Buffer>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.peel_to_id_in_place_packed({{packed}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_id_packed(packed: Opt<Buffer>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.peel_to_id_packed({{packed}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_fully_peeled_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.into_fully_peeled_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_kind(kind: Kind) -> Res!<Object>
      lowers_to
        rust: "{{self}}.peel_to_kind({{kind}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_commit() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.peel_to_commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_tag() -> Res!<Tag>
      lowers_to
        rust: "{{self}}.peel_to_tag({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.peel_to_tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_blob() -> Res!<Blob>
      lowers_to
        rust: "{{self}}.peel_to_blob({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_kind_packed(kind: Kind, packed: Opt<Buffer>) -> Res!<Object>
      lowers_to
        rust: "{{self}}.peel_to_kind_packed({{kind}}, {{packed}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn follow_to_object() -> Res!<Id>
      lowers_to
        rust: "{{self}}.follow_to_object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn follow_to_object_packed(packed: Opt<Buffer>) -> Res!<Id>
      lowers_to
        rust: "{{self}}.follow_to_object_packed({{packed}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn follow() -> Opt<Res!<Reference>>
      lowers_to
        rust: "{{self}}.follow({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Spec
    path revision
    fn from_bstr(spec: BStr, repo: Repository, opts: Options) -> Res!<Self>
      lowers_to
        rust: "gix::Spec::from_bstr({{spec}}, {{repo}}, {{opts}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_id(id: Id) -> Self
      lowers_to
        rust: "gix::Spec::from_id({{id}})"
    fn detach() -> Spec
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn into_references() -> (Opt<Reference>, Opt<Reference>)
      lowers_to
        rust: "{{self}}.into_references({{args}})"
    fn path_and_mode() -> Opt<(BStr, EntryMode)>
      lowers_to
        rust: "{{self}}.path_and_mode({{args}})"
    fn first_reference() -> Opt<Reference>
      lowers_to
        rust: "{{self}}.first_reference({{args}})"
    fn second_reference() -> Opt<Reference>
      lowers_to
        rust: "{{self}}.second_reference({{args}})"
    fn single() -> Opt<Id>
      lowers_to
        rust: "{{self}}.single({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct EntryRef
    path object::tree
    fn mode() -> EntryMode
      lowers_to
        rust: "{{self}}.mode({{args}})"
    fn kind() -> EntryKind
      lowers_to
        rust: "{{self}}.kind({{args}})"
    fn filename() -> BStr
      lowers_to
        rust: "{{self}}.filename({{args}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn oid() -> oid
      lowers_to
        rust: "{{self}}.oid({{args}})"
    fn object() -> Res!<Object>
      lowers_to
        rust: "{{self}}.object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn object_id() -> ObjectId
      lowers_to
        rust: "{{self}}.object_id({{args}})"
    fn detach() -> EntryRef
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn to_owned() -> Entry
      lowers_to
        rust: "{{self}}.to_owned({{args}})"

  struct Resolution
    path commit::describe
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format_with_dirty_suffix(dirty_suffix: Opt<Str>) -> Res!<Format>
      lowers_to
        rust: "{{self}}.format_with_dirty_suffix({{dirty_suffix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Entry
    path object::tree
    fn mode() -> EntryMode
      lowers_to
        rust: "{{self}}.mode({{args}})"
    fn filename() -> BStr
      lowers_to
        rust: "{{self}}.filename({{args}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn object() -> Res!<Object>
      lowers_to
        rust: "{{self}}.object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn oid() -> oid
      lowers_to
        rust: "{{self}}.oid({{args}})"
    fn object_id() -> ObjectId
      lowers_to
        rust: "{{self}}.object_id({{args}})"
    fn detach() -> Entry
      lowers_to
        rust: "{{self}}.detach({{args}})"

  struct Item
    path dirwalk::iter
    fn summary() -> Opt<Summary>
      lowers_to
        rust: "{{self}}.summary({{args}})"
    fn rela_path() -> BStr
      lowers_to
        rust: "{{self}}.rela_path({{args}})"
    fn location() -> BStr
      lowers_to
        rust: "{{self}}.location({{args}})"

  struct Outcome
    path dirwalk
    fn has_changes() -> Bool
      lowers_to
        rust: "{{self}}.has_changes({{args}})"
    fn write_changes() -> Opt<Res!<()>>
      lowers_to
        rust: "{{self}}.write_changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Deregister
    path interrupt::init
    fn deregister() -> Res!<()>
      lowers_to
        rust: "{{self}}.deregister({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_reset(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.with_reset({{toggle}})"
    fn auto_deregister() -> AutoDeregister
      lowers_to
        rust: "{{self}}.auto_deregister({{args}})"

  struct Worktree
    path types
    fn open_index() -> Res!<File>
      lowers_to
        rust: "{{self}}.open_index({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index() -> Res!<Index>
      lowers_to
        rust: "{{self}}.index({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn excludes(overrides: Opt<Search>) -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.excludes({{overrides}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn attributes(overrides: Opt<Search>) -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.attributes({{overrides}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn attributes_only() -> Res!<AttributeStack>
      lowers_to
        rust: "{{self}}.attributes_only({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pathspec(patterns: Unknown) -> Res!<Pathspec>
      lowers_to
        rust: "{{self}}.pathspec({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn base() -> Path
      lowers_to
        rust: "{{self}}.base({{args}})"
    fn is_main() -> Bool
      lowers_to
        rust: "{{self}}.is_main({{args}})"
    fn is_locked() -> Bool
      lowers_to
        rust: "{{self}}.is_locked({{args}})"
    fn lock_reason() -> Opt<BString>
      lowers_to
        rust: "{{self}}.lock_reason({{args}})"
    fn id() -> Opt<BStr>
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn dot_git_exists() -> Bool
      lowers_to
        rust: "{{self}}.dot_git_exists({{args}})"

  struct AttributeStack
    path types
    fn new(stack: Stack, repo: Repository) -> Self
      lowers_to
        rust: "gix::AttributeStack::new({{stack}}, {{repo}})"
    fn detach() -> Stack
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn at_path(relative: Path, mode: Opt<Mode>) -> Res!<Platform>
      lowers_to
        rust: "{{self}}.at_path({{relative}}, {{mode}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn at_entry(relative: Unknown, mode: Opt<Mode>) -> Res!<Platform>
      lowers_to
        rust: "{{self}}.at_entry({{relative}}, {{mode}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Pathspec
    path types
    fn new(repo: Repository, empty_patterns_match_prefix: Bool, patterns: Unknown, inherit_ignore_case: Bool, make_attributes: Unknown) -> Res!<Self>
      lowers_to
        rust: "gix::Pathspec::new({{repo}}, {{empty_patterns_match_prefix}}, {{patterns}}, {{inherit_ignore_case}}, {{make_attributes}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_parts() -> (Search, Opt<AttributeStack>)
      lowers_to
        rust: "{{self}}.into_parts({{args}})"
    fn detach() -> Res!<PathspecDetached>
      lowers_to
        rust: "{{self}}.detach({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn attributes() -> Opt<Stack>
      lowers_to
        rust: "{{self}}.attributes({{args}})"
    fn search() -> Search
      lowers_to
        rust: "{{self}}.search({{args}})"
    fn pattern_matching_relative_path(relative_path: BStr, is_dir: Opt<Bool>) -> Opt<Match>
      lowers_to
        rust: "{{self}}.pattern_matching_relative_path({{relative_path}}, {{is_dir}})"
    fn is_included(relative_path: BStr, is_dir: Opt<Bool>) -> Bool
      lowers_to
        rust: "{{self}}.is_included({{relative_path}}, {{is_dir}})"
    fn index_entries_with_paths(index: State) -> Opt<Unknown>
      lowers_to
        rust: "{{self}}.index_entries_with_paths({{index}})"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Cursor
    path object::tree::editor
    fn upsert(rela_path: Unknown, kind: EntryKind, id: ObjectId) -> Res!<Self>
      lowers_to
        rust: "{{self}}.upsert({{rela_path}}, {{kind}}, {{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remove(rela_path: Unknown) -> Res!<Self>
      lowers_to
        rust: "{{self}}.remove({{rela_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remove_leaf(rela_path: Unknown) -> Res!<Self>
      lowers_to
        rust: "{{self}}.remove_leaf({{rela_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn write() -> Res!<Id>
      lowers_to
        rust: "{{self}}.write({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn get(rela_path: Unknown) -> Opt<EntryRef>
      lowers_to
        rust: "{{self}}.get({{rela_path}})"

  struct Head
    path types
    fn into_remote(direction: Direction) -> Opt<Res!<Remote>>
      lowers_to
        rust: "{{self}}.into_remote({{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn log_iter() -> Platform
      lowers_to
        rust: "{{self}}.log_iter({{args}})"
    fn prior_checked_out_branches() -> Res!<Opt<List<(BString, ObjectId)>>>
      lowers_to
        rust: "{{self}}.prior_checked_out_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_peeled_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.into_peeled_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_peeled_object() -> Res!<Object>
      lowers_to
        rust: "{{self}}.into_peeled_object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_peeled_id() -> Res!<Opt<Id>>
      lowers_to
        rust: "{{self}}.try_into_peeled_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_peel_to_id_in_place() -> Res!<Opt<Id>>
      lowers_to
        rust: "{{self}}.try_peel_to_id_in_place({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_peel_to_id() -> Res!<Opt<Id>>
      lowers_to
        rust: "{{self}}.try_peel_to_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_object_in_place() -> Res!<Object>
      lowers_to
        rust: "{{self}}.peel_to_object_in_place({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_object() -> Res!<Object>
      lowers_to
        rust: "{{self}}.peel_to_object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_commit_in_place() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.peel_to_commit_in_place({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_commit() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.peel_to_commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn name() -> FullNameRef
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn referent_name() -> Opt<FullNameRef>
      lowers_to
        rust: "{{self}}.referent_name({{args}})"
    fn is_detached() -> Bool
      lowers_to
        rust: "{{self}}.is_detached({{args}})"
    fn is_unborn() -> Bool
      lowers_to
        rust: "{{self}}.is_unborn({{args}})"
    fn id() -> Opt<Id>
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn try_into_referent() -> Opt<Reference>
      lowers_to
        rust: "{{self}}.try_into_referent({{args}})"

  struct Remote
    path config::tree::sections
    fn with_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn push_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.push_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_push_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_push_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn push_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.push_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_push_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_push_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_fetch_tags(tags: Tags) -> Self
      lowers_to
        rust: "{{self}}.with_fetch_tags({{tags}})"
    fn with_refspecs(specs: Unknown, direction: Direction) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_refspecs({{specs}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn save_to(config: File) -> Res!<()>
      lowers_to
        rust: "{{self}}.save_to({{config}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn save_as_to(name: Unknown, config: File) -> Res!<()>
      lowers_to
        rust: "{{self}}.save_as_to({{name}}, {{config}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn name() -> Opt<Name>
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn repo() -> Repository
      lowers_to
        rust: "{{self}}.repo({{args}})"
    fn refspecs(direction: Direction) -> List<RefSpec>
      lowers_to
        rust: "{{self}}.refspecs({{direction}})"
    fn fetch_tags() -> Tags
      lowers_to
        rust: "{{self}}.fetch_tags({{args}})"
    fn url(direction: Direction) -> Opt<Url>
      lowers_to
        rust: "{{self}}.url({{direction}})"
    fn urls(direction: Direction) -> Unknown
      lowers_to
        rust: "{{self}}.urls({{direction}})"
    fn rewrite_urls() -> Res!<Self>
      lowers_to
        rust: "{{self}}.rewrite_urls({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn replace_refspecs(specs: Unknown, direction: Direction) -> Res!<()>
      lowers_to
        rust: "{{self}}.replace_refspecs({{specs}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Stats
    path object::tree::diff

  struct Object
    path types
    fn peel_to_kind(kind: Kind) -> Res!<Self>
      lowers_to
        rust: "{{self}}.peel_to_kind({{kind}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.peel_to_tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_commit() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.peel_to_commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_tags_to_end() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peel_tags_to_end({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_blob() -> Blob
      lowers_to
        rust: "{{self}}.into_blob({{args}})"
    fn into_tree() -> Tree
      lowers_to
        rust: "{{self}}.into_tree({{args}})"
    fn into_commit() -> Commit
      lowers_to
        rust: "{{self}}.into_commit({{args}})"
    fn into_tag() -> Tag
      lowers_to
        rust: "{{self}}.into_tag({{args}})"
    fn try_into_commit() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.try_into_commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_tag() -> Res!<Tag>
      lowers_to
        rust: "{{self}}.try_into_tag({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.try_into_tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_blob() -> Res!<Blob>
      lowers_to
        rust: "{{self}}.try_into_blob({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn to_commit_ref() -> CommitRef
      lowers_to
        rust: "{{self}}.to_commit_ref({{args}})"
    fn try_to_commit_ref() -> Res!<CommitRef>
      lowers_to
        rust: "{{self}}.try_to_commit_ref({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn to_commit_ref_iter() -> CommitRefIter
      lowers_to
        rust: "{{self}}.to_commit_ref_iter({{args}})"
    fn try_to_commit_ref_iter() -> Opt<CommitRefIter>
      lowers_to
        rust: "{{self}}.try_to_commit_ref_iter({{args}})"
    fn to_tag_ref_iter() -> TagRefIter
      lowers_to
        rust: "{{self}}.to_tag_ref_iter({{args}})"
    fn try_to_tag_ref_iter() -> Opt<TagRefIter>
      lowers_to
        rust: "{{self}}.try_to_tag_ref_iter({{args}})"
    fn to_tag_ref() -> TagRef
      lowers_to
        rust: "{{self}}.to_tag_ref({{args}})"
    fn try_to_tag_ref() -> Res!<TagRef>
      lowers_to
        rust: "{{self}}.try_to_tag_ref({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"

  struct BreadthFirstPresets
    path object::tree::traverse
    fn files() -> Res!<List<Entry>>
      lowers_to
        rust: "{{self}}.files({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Commit
    path config::tree::sections::gitoxide::subsections
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"
    fn short_id() -> Res!<Prefix>
      lowers_to
        rust: "{{self}}.short_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message() -> Res!<MessageRef>
      lowers_to
        rust: "{{self}}.message({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message_raw() -> Res!<BStr>
      lowers_to
        rust: "{{self}}.message_raw({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message_raw_sloppy() -> BStr
      lowers_to
        rust: "{{self}}.message_raw_sloppy({{args}})"
    fn time() -> Res!<Time>
      lowers_to
        rust: "{{self}}.time({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn decode() -> Res!<CommitRef>
      lowers_to
        rust: "{{self}}.decode({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn iter() -> CommitRefIter
      lowers_to
        rust: "{{self}}.iter({{args}})"
    fn author() -> Res!<SignatureRef>
      lowers_to
        rust: "{{self}}.author({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn committer() -> Res!<SignatureRef>
      lowers_to
        rust: "{{self}}.committer({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn parent_ids() -> Unknown
      lowers_to
        rust: "{{self}}.parent_ids({{args}})"
    fn tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tree_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.tree_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn ancestors() -> Platform
      lowers_to
        rust: "{{self}}.ancestors({{args}})"
    fn describe() -> Platform
      lowers_to
        rust: "{{self}}.describe({{args}})"
    fn signature() -> Res!<Opt<(Cow<BStr>, SignedData)>>
      lowers_to
        rust: "{{self}}.signature({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Iter
    path dirwalk
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn peeled() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peeled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(inner: I, make_err: EFN) -> Self
      lowers_to
        rust: "gix::Iter::new({{inner}}, {{make_err}})"
    fn into_inner() -> I
      lowers_to
        rust: "{{self}}.into_inner({{args}})"
    fn inner() -> I
      lowers_to
        rust: "{{self}}.inner({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct ObjectDetached
    path types
    fn attach(repo: Repository) -> Object
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  struct Tree
    path config::tree::root
    fn edit() -> Res!<Editor>
      lowers_to
        rust: "{{self}}.edit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn changes() -> Res!<Platform>
      lowers_to
        rust: "{{self}}.changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn traverse() -> Platform
      lowers_to
        rust: "{{self}}.traverse({{args}})"
    fn iter() -> Unknown
      lowers_to
        rust: "{{self}}.iter({{args}})"
    fn from_data(id: ObjectId, data: List<Int>, repo: Repository) -> Self
      lowers_to
        rust: "gix::Tree::from_data({{id}}, {{data}}, {{repo}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn decode() -> Res!<TreeRef>
      lowers_to
        rust: "{{self}}.decode({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_entry(name: Unknown) -> Opt<EntryRef>
      lowers_to
        rust: "{{self}}.find_entry({{name}})"
    fn lookup_entry(path: I) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.lookup_entry({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_entry(path: I) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.peel_to_entry({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lookup_entry_by_path(relative_path: Path) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.lookup_entry_by_path({{relative_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_entry_by_path(relative_path: Path) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.peel_to_entry_by_path({{relative_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"
    fn sections() -> List<Unknown>
      lowers_to
        rust: "{{self}}.sections({{args}})"

  struct Blob
    path types
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"

  struct Tag
    path types
    fn decode() -> Res!<TagRef>
      lowers_to
        rust: "{{self}}.decode({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn target_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.target_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tagger() -> Res!<Opt<SignatureRef>>
      lowers_to
        rust: "{{self}}.tagger({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Editor
    path object::tree
    fn new(tree: Tree) -> Res!<Self>
      lowers_to
        rust: "gix::Editor::new({{tree}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn detach() -> Editor
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn to_cursor() -> Cursor
      lowers_to
        rust: "{{self}}.to_cursor({{args}})"
    fn cursor_at(rela_path: Unknown) -> Res!<Cursor>
      lowers_to
        rust: "{{self}}.cursor_at({{rela_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn set_root(root: Tree) -> Res!<Self>
      lowers_to
        rust: "{{self}}.set_root({{root}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn upsert(rela_path: Unknown, kind: EntryKind, id: ObjectId) -> Res!<Self>
      lowers_to
        rust: "{{self}}.upsert({{rela_path}}, {{kind}}, {{id}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remove(rela_path: Unknown) -> Res!<Self>
      lowers_to
        rust: "{{self}}.remove({{rela_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remove_leaf(rela_path: Unknown) -> Res!<Self>
      lowers_to
        rust: "{{self}}.remove_leaf({{rela_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn write() -> Res!<Id>
      lowers_to
        rust: "{{self}}.write({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn get(rela_path: Unknown) -> Opt<EntryRef>
      lowers_to
        rust: "{{self}}.get({{rela_path}})"

  struct Config
    path open::permissions
    fn all() -> Self
      lowers_to
        rust: "gix::Config::all({{args}})"
    fn isolated() -> Self
      lowers_to
        rust: "gix::Config::isolated({{args}})"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Attributes
    path open::permissions
    fn all() -> Self
      lowers_to
        rust: "gix::Attributes::all({{args}})"
    fn isolated() -> Self
      lowers_to
        rust: "gix::Attributes::isolated({{args}})"

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Environment
    path open::permissions
    fn all() -> Self
      lowers_to
        rust: "gix::Environment::all({{args}})"
    fn isolated() -> Self
      lowers_to
        rust: "gix::Environment::isolated({{args}})"

  struct Permissions
    path open
    fn secure() -> Self
      lowers_to
        rust: "gix::Permissions::secure({{args}})"
    fn all() -> Self
      lowers_to
        rust: "gix::Permissions::all({{args}})"
    fn isolated() -> Self
      lowers_to
        rust: "gix::Permissions::isolated({{args}})"
    fn is_isolated() -> Bool
      lowers_to
        rust: "{{self}}.is_isolated({{args}})"

  struct Tree
    path config::tree::root
    fn edit() -> Res!<Editor>
      lowers_to
        rust: "{{self}}.edit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn changes() -> Res!<Platform>
      lowers_to
        rust: "{{self}}.changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn traverse() -> Platform
      lowers_to
        rust: "{{self}}.traverse({{args}})"
    fn iter() -> Unknown
      lowers_to
        rust: "{{self}}.iter({{args}})"
    fn from_data(id: ObjectId, data: List<Int>, repo: Repository) -> Self
      lowers_to
        rust: "gix::Tree::from_data({{id}}, {{data}}, {{repo}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn decode() -> Res!<TreeRef>
      lowers_to
        rust: "{{self}}.decode({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn find_entry(name: Unknown) -> Opt<EntryRef>
      lowers_to
        rust: "{{self}}.find_entry({{name}})"
    fn lookup_entry(path: I) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.lookup_entry({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_entry(path: I) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.peel_to_entry({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lookup_entry_by_path(relative_path: Path) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.lookup_entry_by_path({{relative_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn peel_to_entry_by_path(relative_path: Path) -> Res!<Opt<Entry>>
      lowers_to
        rust: "{{self}}.peel_to_entry_by_path({{relative_path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"
    fn sections() -> List<Unknown>
      lowers_to
        rust: "{{self}}.sections({{args}})"

  struct Author
    path config::tree::sections::gitoxide::subsections

  struct Branch
    path config::tree::sections

  struct Checkout
    path config::tree::sections

  struct Clone
    path config::tree::sections

  struct Committer
    path config::tree::sections::gitoxide::subsections

  struct Core
    path config::tree::sections::gitoxide::subsections

  struct Credential
    path config::tree::sections

  struct Diff
    path config::tree::sections

  struct Extensions
    path config::tree::sections

  struct Fetch
    path config::tree::sections

  struct Gitoxide
    path config::tree::sections

  struct Http
    path config::tree::sections::gitoxide::subsections

  struct Index
    path config::tree::sections

  struct Init
    path config::tree::sections

  struct Mailmap
    path config::tree::sections

  struct Merge
    path config::tree::sections

  struct Pack
    path config::tree::sections

  struct Protocol
    path config::tree::sections

  struct Push
    path config::tree::sections

  struct Remote
    path config::tree::sections
    fn with_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn push_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.push_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_push_url(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_push_url({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn push_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.push_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_push_url_without_url_rewrite(url: Url) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_push_url_without_url_rewrite({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn with_fetch_tags(tags: Tags) -> Self
      lowers_to
        rust: "{{self}}.with_fetch_tags({{tags}})"
    fn with_refspecs(specs: Unknown, direction: Direction) -> Res!<Self>
      lowers_to
        rust: "{{self}}.with_refspecs({{specs}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn save_to(config: File) -> Res!<()>
      lowers_to
        rust: "{{self}}.save_to({{config}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn save_as_to(name: Unknown, config: File) -> Res!<()>
      lowers_to
        rust: "{{self}}.save_as_to({{name}}, {{config}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn name() -> Opt<Name>
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn repo() -> Repository
      lowers_to
        rust: "{{self}}.repo({{args}})"
    fn refspecs(direction: Direction) -> List<RefSpec>
      lowers_to
        rust: "{{self}}.refspecs({{direction}})"
    fn fetch_tags() -> Tags
      lowers_to
        rust: "{{self}}.fetch_tags({{args}})"
    fn url(direction: Direction) -> Opt<Url>
      lowers_to
        rust: "{{self}}.url({{direction}})"
    fn urls(direction: Direction) -> Unknown
      lowers_to
        rust: "{{self}}.urls({{direction}})"
    fn rewrite_urls() -> Res!<Self>
      lowers_to
        rust: "{{self}}.rewrite_urls({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn replace_refspecs(specs: Unknown, direction: Direction) -> Res!<()>
      lowers_to
        rust: "{{self}}.replace_refspecs({{specs}}, {{direction}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Safe
    path config::tree::sections
    fn directory_filter(meta: Metadata) -> Bool
      lowers_to
        rust: "gix::Safe::directory_filter({{meta}})"

  struct Ssh
    path config::tree::sections::gitoxide::subsections

  struct Status
    path config::tree::sections
    fn is_dirty() -> Opt<Bool>
      lowers_to
        rust: "{{self}}.is_dirty({{args}})"

  struct User
    path config::tree::sections::gitoxide::subsections

  struct Url
    path config::tree::keys::validate

  struct FullNameRef
    path config::tree::sections::branch::validate

  struct Any
    path config::tree::keys
    fn try_into_fullrefname(value: Unknown) -> Res!<FullName>
      lowers_to
        rust: "gix::Any::try_into_fullrefname({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_from_workers(value: Res!<Opt<Int>>) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_from_workers({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_encodings(value: Opt<Unknown>) -> Res!<List<Encoding>>
      lowers_to
        rust: "{{self}}.try_into_encodings({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_eol(value: Unknown) -> Res!<Mode>
      lowers_to
        rust: "{{self}}.try_into_eol({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_safecrlf(value: Unknown) -> Res!<CrlfRoundTripCheck>
      lowers_to
        rust: "{{self}}.try_into_safecrlf({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_autocrlf(value: Unknown) -> Res!<AutoCrlf>
      lowers_to
        rust: "{{self}}.try_into_autocrlf({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_object_kind_hint(value: Unknown) -> Res!<Opt<ObjectKindHint>>
      lowers_to
        rust: "{{self}}.try_into_object_kind_hint({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_ref_updates(value: Res!<Opt<Bool>>) -> Res!<Opt<WriteReflog>>
      lowers_to
        rust: "{{self}}.try_into_ref_updates({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_checkstat(value: Unknown) -> Res!<Bool>
      lowers_to
        rust: "{{self}}.try_into_checkstat({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_abbreviation(hex_len_str: Unknown, object_hash: Kind) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_into_abbreviation({{hex_len_str}}, {{object_hash}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_ignore(value: Unknown) -> Res!<Ignore>
      lowers_to
        rust: "{{self}}.try_into_ignore({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_algorithm(name: Unknown) -> Res!<Algorithm>
      lowers_to
        rust: "{{self}}.try_into_algorithm({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_binary(value: Opt<Unknown>) -> Res!<Opt<Bool>>
      lowers_to
        rust: "{{self}}.try_into_binary({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_renames(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_renames({{name}}, {{section}})"
    fn try_into_renames(value: Res!<Opt<Bool>>) -> Res!<Opt<Tracking>>
      lowers_to
        rust: "{{self}}.try_into_renames({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_object_format(value: Unknown) -> Res!<Kind>
      lowers_to
        rust: "{{self}}.try_into_object_format({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_negotiation_algorithm(name: Unknown) -> Res!<Algorithm>
      lowers_to
        rust: "{{self}}.try_into_negotiation_algorithm({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_recurse_submodules(value: Res!<Opt<Bool>>) -> Res!<Opt<FetchRecurse>>
      lowers_to
        rust: "{{self}}.try_into_recurse_submodules({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_refs_namespace(name: Unknown) -> Res!<Namespace>
      lowers_to
        rust: "{{self}}.try_into_refs_namespace({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_ssl_version(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_ssl_version({{name}}, {{section}})"
    fn new_proxy_auth_method(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_proxy_auth_method({{name}}, {{section}})"
    fn try_into_extra_header(values: List<Unknown>) -> Res!<List<Str>>
      lowers_to
        rust: "{{self}}.try_into_extra_header({{values}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_index_threads(value: Unknown) -> Res!<Int>
      lowers_to
        rust: "{{self}}.try_into_index_threads({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_index_version(value: Res!<Opt<Int>>) -> Res!<Opt<Version>>
      lowers_to
        rust: "{{self}}.try_into_index_version({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_default(value: Unknown) -> Res!<Default>
      lowers_to
        rust: "{{self}}.try_into_default({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_tag_opt(value: Unknown) -> Res!<Tags>
      lowers_to
        rust: "{{self}}.try_into_tag_opt({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_show_untracked_files(value: Unknown) -> Res!<UntrackedFiles>
      lowers_to
        rust: "{{self}}.try_into_show_untracked_files({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_duration(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_duration({{name}}, {{section}})"
    fn try_into_duration(value: Res!<Opt<Int>>) -> Res!<Opt<Duration>>
      lowers_to
        rust: "{{self}}.try_into_duration({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_lock_timeout(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_lock_timeout({{name}}, {{section}})"
    fn try_into_lock_timeout(value: Res!<Opt<Int>>) -> Res!<Opt<Fail>>
      lowers_to
        rust: "{{self}}.try_into_lock_timeout({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_compression(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_compression({{name}}, {{section}})"
    fn try_into_compression(value: Res!<Opt<Int>>) -> Res!<Opt<Compression>>
      lowers_to
        rust: "{{self}}.try_into_compression({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_push_refspec(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_push_refspec({{name}}, {{section}})"
    fn new_fetch_refspec(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_fetch_refspec({{name}}, {{section}})"
    fn new_url(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_url({{name}}, {{section}})"
    fn try_into_url(value: Unknown) -> Res!<Url>
      lowers_to
        rust: "{{self}}.try_into_url({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_unsigned_integer(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_unsigned_integer({{name}}, {{section}})"
    fn try_into_usize(value: Res!<Opt<Int>>) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_into_usize({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_u64(value: Res!<Opt<Int>>) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_into_u64({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_u32(value: Res!<Opt<Int>>) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_into_u32({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_time(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_time({{name}}, {{section}})"
    fn try_into_time(value: Unknown, now: Opt<SystemTime>) -> Res!<Time>
      lowers_to
        rust: "{{self}}.try_into_time({{value}}, {{now}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_boolean(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_boolean({{name}}, {{section}})"
    fn enrich_error(value: Res!<Opt<Bool>>) -> Res!<Opt<Bool>>
      lowers_to
        rust: "{{self}}.enrich_error({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_remote_name(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_remote_name({{name}}, {{section}})"
    fn try_into_symbolic_name(name: Unknown) -> Res!<BString>
      lowers_to
        rust: "{{self}}.try_into_symbolic_name({{name}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new({{name}}, {{section}})"
    fn new_with_validate(name: Str, section: Unknown, validate: T) -> Self
      lowers_to
        rust: "gix::Any::new_with_validate({{name}}, {{section}}, {{validate}})"
    fn with_subsection_requirement(requirement: Opt<SubSectionRequirement>) -> Self
      lowers_to
        rust: "{{self}}.with_subsection_requirement({{requirement}})"
    fn with_environment_override(var: Str) -> Self
      lowers_to
        rust: "{{self}}.with_environment_override({{var}})"
    fn with_fallback(key: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_fallback({{key}})"
    fn with_note(message: Str) -> Self
      lowers_to
        rust: "{{self}}.with_note({{message}})"
    fn with_deviation(message: Str) -> Self
      lowers_to
        rust: "{{self}}.with_deviation({{message}})"
    fn try_into_refspec(value: Unknown, op: Operation) -> Res!<RefSpec>
      lowers_to
        rust: "{{self}}.try_into_refspec({{value}}, {{op}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_into_string(value: Unknown) -> Res!<Str>
      lowers_to
        rust: "{{self}}.try_into_string({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new_string(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_string({{name}}, {{section}})"
    fn new_program(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_program({{name}}, {{section}})"
    fn new_executable(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_executable({{name}}, {{section}})"
    fn new_path(name: Str, section: Unknown) -> Self
      lowers_to
        rust: "gix::Any::new_path({{name}}, {{section}})"

  struct Workers
    path config::tree::sections::checkout::validate

  struct SafeCrlf
    path config::tree::sections::core::validate

  struct AutoCrlf
    path config::tree::sections::core::validate

  struct Eol
    path config::tree::sections::core::validate

  struct CheckRoundTripEncoding
    path config::tree::sections::core::validate

  struct Disambiguate
    path config::tree::sections::core::validate

  struct LogAllRefUpdates
    path config::tree::sections::core::validate

  struct CheckStat
    path config::tree::sections::core::validate

  struct Abbrev
    path config::tree::sections::core::validate

  struct UrlParameter
    path config::tree::sections::credential

  struct Ignore
    path config::tree::sections::diff::validate

  struct Algorithm
    path config::tree::sections::diff::validate

  struct Renames
    path config::tree::sections::diff::validate

  struct Binary
    path config::tree::sections::diff::validate

  struct ObjectFormat
    path config::tree::sections::extensions::validate

  struct NegotiationAlgorithm
    path config::tree::sections::fetch::validate

  struct RecurseSubmodules
    path config::tree::sections::fetch::validate

  struct Core
    path config::tree::sections::gitoxide::subsections

  struct RefsNamespace
    path config::tree::sections::gitoxide::validate

  struct Http
    path config::tree::sections::gitoxide::subsections

  struct Https
    path config::tree::sections::gitoxide::subsections

  struct Allow
    path config::tree::sections::protocol::validate

  struct ProtocolFromUser
    path config::tree::sections::gitoxide::validate

  struct Author
    path config::tree::sections::gitoxide::subsections

  struct User
    path config::tree::sections::gitoxide::subsections

  struct Ssh
    path config::tree::sections::gitoxide::subsections

  struct Objects
    path config::tree::sections::gitoxide::subsections

  struct Committer
    path config::tree::sections::gitoxide::subsections

  struct Credentials
    path config::tree::sections::gitoxide::subsections

  struct Commit
    path config::tree::sections::gitoxide::subsections
    fn detached() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detached({{args}})"
    fn detach() -> ObjectDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn take_data() -> List<Int>
      lowers_to
        rust: "{{self}}.take_data({{args}})"
    fn short_id() -> Res!<Prefix>
      lowers_to
        rust: "{{self}}.short_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message() -> Res!<MessageRef>
      lowers_to
        rust: "{{self}}.message({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message_raw() -> Res!<BStr>
      lowers_to
        rust: "{{self}}.message_raw({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn message_raw_sloppy() -> BStr
      lowers_to
        rust: "{{self}}.message_raw_sloppy({{args}})"
    fn time() -> Res!<Time>
      lowers_to
        rust: "{{self}}.time({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn decode() -> Res!<CommitRef>
      lowers_to
        rust: "{{self}}.decode({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn iter() -> CommitRefIter
      lowers_to
        rust: "{{self}}.iter({{args}})"
    fn author() -> Res!<SignatureRef>
      lowers_to
        rust: "{{self}}.author({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn committer() -> Res!<SignatureRef>
      lowers_to
        rust: "{{self}}.committer({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn parent_ids() -> Unknown
      lowers_to
        rust: "{{self}}.parent_ids({{args}})"
    fn tree() -> Res!<Tree>
      lowers_to
        rust: "{{self}}.tree({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tree_id() -> Res!<Id>
      lowers_to
        rust: "{{self}}.tree_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn ancestors() -> Platform
      lowers_to
        rust: "{{self}}.ancestors({{args}})"
    fn describe() -> Platform
      lowers_to
        rust: "{{self}}.describe({{args}})"
    fn signature() -> Res!<Opt<(Cow<BStr>, SignedData)>>
      lowers_to
        rust: "{{self}}.signature({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct SslVersion
    path config::tree::sections::http::validate

  struct ProxyAuthMethod
    path config::tree::sections::http::validate

  struct Version
    path config::tree::sections::protocol::validate

  struct Iter
    path dirwalk
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn peeled() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peeled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(inner: I, make_err: EFN) -> Self
      lowers_to
        rust: "gix::Iter::new({{inner}}, {{make_err}})"
    fn into_inner() -> I
      lowers_to
        rust: "{{self}}.into_inner({{args}})"
    fn inner() -> I
      lowers_to
        rust: "{{self}}.inner({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"

  struct ExtraHeader
    path config::tree::sections::http::validate

  struct FollowRedirects
    path config::tree::sections::http::validate

  struct IndexThreads
    path config::tree::sections::index::validate

  struct IndexVersion
    path config::tree::sections::pack::validate

  struct Allow
    path config::tree::sections::protocol::validate

  struct Version
    path config::tree::sections::protocol::validate

  struct NameParameter
    path config::tree::sections::protocol

  struct Platform
    path commit::describe
    fn all() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn prefixed(prefix: RelativePath) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.prefixed({{prefix}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn tags() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.tags({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn local_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.local_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn pseudo() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.pseudo({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn remote_branches() -> Res!<Iter>
      lowers_to
        rust: "{{self}}.remote_branches({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn dirwalk_options(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options({{cb}})"
    fn dirwalk_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.dirwalk_options_mut({{cb}})"
    fn untracked_files(untracked_files: UntrackedFiles) -> Self
      lowers_to
        rust: "{{self}}.untracked_files({{untracked_files}})"
    fn should_interrupt_shared(should_interrupt: AtomicBool) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_shared({{should_interrupt}})"
    fn should_interrupt_owned(should_interrupt: Arc<AtomicBool>) -> Self
      lowers_to
        rust: "{{self}}.should_interrupt_owned({{should_interrupt}})"
    fn index_worktree_submodules(submodules: Opt<Submodule>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_submodules({{submodules}})"
    fn index(index: IndexPersistedOrInMemory) -> Self
      lowers_to
        rust: "{{self}}.index({{index}})"
    fn index_worktree_rewrites(rewrites: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_rewrites({{rewrites}})"
    fn index_worktree_options_mut(cb: Unknown) -> Self
      lowers_to
        rust: "{{self}}.index_worktree_options_mut({{cb}})"
    fn head_tree(tree: ObjectId) -> Self
      lowers_to
        rust: "{{self}}.head_tree({{tree}})"
    fn tree_index_track_renames(renames: TrackRenames) -> Self
      lowers_to
        rust: "{{self}}.tree_index_track_renames({{renames}})"
    fn into_index_worktree_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_index_worktree_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_iter(patterns: Unknown) -> Res!<Iter>
      lowers_to
        rust: "{{self}}.into_iter({{patterns}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn lines(process_hunk: FnH) -> Res!<Outcome>
      lowers_to
        rust: "{{self}}.lines({{process_hunk}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn line_counts() -> Res!<Opt<DiffLineStats>>
      lowers_to
        rust: "{{self}}.line_counts({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn sorting(sorting: Sorting) -> Self
      lowers_to
        rust: "{{self}}.sorting({{sorting}})"
    fn first_parent_only() -> Self
      lowers_to
        rust: "{{self}}.first_parent_only({{args}})"
    fn use_commit_graph(toggle: Opt<Bool>) -> Self
      lowers_to
        rust: "{{self}}.use_commit_graph({{toggle}})"
    fn with_commit_graph(graph: Opt<Graph>) -> Self
      lowers_to
        rust: "{{self}}.with_commit_graph({{graph}})"
    fn with_boundary(ids: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_boundary({{ids}})"
    fn with_hidden(tips: Unknown) -> Self
      lowers_to
        rust: "{{self}}.with_hidden({{tips}})"
    fn selected(filter: Unknown) -> Res!<Walk>
      lowers_to
        rust: "{{self}}.selected({{filter}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all() -> Res!<Walk>
      lowers_to
        rust: "{{self}}.all({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree(other: Tree, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree({{other}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn for_each_to_obtain_tree_with_cache(other: Tree, resource_cache: Platform, for_each: Unknown) -> Res!<Opt<Outcome>>
      lowers_to
        rust: "{{self}}.for_each_to_obtain_tree_with_cache({{other}}, {{resource_cache}}, {{for_each}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn options(change_opts: Unknown) -> Self
      lowers_to
        rust: "{{self}}.options({{change_opts}})"
    fn stats(other: Tree) -> Res!<Stats>
      lowers_to
        rust: "{{self}}.stats({{other}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn breadthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.breadthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn depthfirst(delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.depthfirst({{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn names(select: SelectRef) -> Self
      lowers_to
        rust: "{{self}}.names({{select}})"
    fn traverse_first_parent(first_parent: Bool) -> Self
      lowers_to
        rust: "{{self}}.traverse_first_parent({{first_parent}})"
    fn max_candidates(candidates: Int) -> Self
      lowers_to
        rust: "{{self}}.max_candidates({{candidates}})"
    fn id_as_fallback(use_fallback: Bool) -> Self
      lowers_to
        rust: "{{self}}.id_as_fallback({{use_fallback}})"
    fn try_format() -> Res!<Opt<Format>>
      lowers_to
        rust: "{{self}}.try_format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve_with_cache(cache: Opt<Graph>) -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve_with_cache({{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_resolve() -> Res!<Opt<Resolution>>
      lowers_to
        rust: "{{self}}.try_resolve({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn format() -> Res!<Format>
      lowers_to
        rust: "{{self}}.format({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Default
    path config::tree::sections::push::validate

  struct TagOpt
    path config::tree::sections::remote::validate

  struct Variant
    path config::tree::sections::ssh::validate

  struct ShowUntrackedFiles
    path config::tree::sections::status::validate

  struct All
    path config::tree::keys::validate

  struct Time
    path config::tree::keys::validate

  struct UnsignedInteger
    path config::tree::keys::validate

  struct Boolean
    path config::tree::keys::validate

  struct RemoteName
    path config::tree::keys::validate

  struct Program
    path config::tree::keys::validate

  struct Executable
    path config::tree::keys::validate

  struct Url
    path config::tree::keys::validate

  struct PushRefSpec
    path config::tree::keys::validate

  struct FetchRefSpec
    path config::tree::keys::validate

  struct LockTimeout
    path config::tree::keys::validate

  struct Compression
    path config::tree::keys::validate

  struct DurationInMilliseconds
    path config::tree::keys::validate

  struct String
    path config::tree::keys::validate

  struct Path
    path config::tree::keys::validate

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct AutoDeregister
    path interrupt::init

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Snapshot
    path config
    fn boolean(key: Unknown) -> Opt<Bool>
      lowers_to
        rust: "{{self}}.boolean({{key}})"
    fn try_boolean(key: Unknown) -> Res!<Opt<Bool>>
      lowers_to
        rust: "{{self}}.try_boolean({{key}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn integer(key: Unknown) -> Opt<Int>
      lowers_to
        rust: "{{self}}.integer({{key}})"
    fn try_integer(key: Unknown) -> Res!<Opt<Int>>
      lowers_to
        rust: "{{self}}.try_integer({{key}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn string(key: Unknown) -> Opt<BString>
      lowers_to
        rust: "{{self}}.string({{key}})"
    fn trusted_path(key: Unknown) -> Res!<Opt<PathBuf>>
      lowers_to
        rust: "{{self}}.trusted_path({{key}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn trusted_program(key: Unknown) -> Opt<OsString>
      lowers_to
        rust: "{{self}}.trusted_program({{key}})"
    fn plumbing() -> File
      lowers_to
        rust: "{{self}}.plumbing({{args}})"
    fn credential_helpers(url: Url) -> Res!<(Cascade, Action, Options)>
      lowers_to
        rust: "{{self}}.credential_helpers({{url}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct SnapshotMut
    path config
    fn append_config(values: Unknown, source: Source) -> Res!<Self>
      lowers_to
        rust: "{{self}}.append_config({{values}}, {{source}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn commit() -> Res!<Repository>
      lowers_to
        rust: "{{self}}.commit({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn set_value(key: Unknown, new_value: Unknown) -> Res!<Opt<BString>>
      lowers_to
        rust: "{{self}}.set_value({{key}}, {{new_value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn set_subsection_value(key: Unknown, subsection: Unknown, new_value: Unknown) -> Res!<Opt<BString>>
      lowers_to
        rust: "{{self}}.set_subsection_value({{key}}, {{subsection}}, {{new_value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn commit_auto_rollback() -> Res!<CommitAutoRollback>
      lowers_to
        rust: "{{self}}.commit_auto_rollback({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn forget() -> File
      lowers_to
        rust: "{{self}}.forget({{args}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Outcome
    path dirwalk
    fn has_changes() -> Bool
      lowers_to
        rust: "{{self}}.has_changes({{args}})"
    fn write_changes() -> Opt<Res!<()>>
      lowers_to
        rust: "{{self}}.write_changes({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct CommitAutoRollback
    path config
    fn rollback() -> Res!<Repository>
      lowers_to
        rust: "{{self}}.rollback({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Iter
    path dirwalk
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"
    fn peeled() -> Res!<Self>
      lowers_to
        rust: "{{self}}.peeled({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(inner: I, make_err: EFN) -> Self
      lowers_to
        rust: "gix::Iter::new({{inner}}, {{make_err}})"
    fn into_inner() -> I
      lowers_to
        rust: "{{self}}.into_inner({{args}})"
    fn inner() -> I
      lowers_to
        rust: "{{self}}.inner({{args}})"
    fn outcome_mut() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.outcome_mut({{args}})"
    fn into_outcome() -> Opt<Outcome>
      lowers_to
        rust: "{{self}}.into_outcome({{args}})"

  struct Pipeline
    path filter
    fn options(repo: Repository) -> Res!<Options>
      lowers_to
        rust: "gix::Pipeline::options({{repo}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn new(repo: Repository, cache: Stack) -> Res!<Self>
      lowers_to
        rust: "gix::Pipeline::new({{repo}}, {{cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn into_parts() -> (Pipeline, Stack)
      lowers_to
        rust: "{{self}}.into_parts({{args}})"
    fn convert_to_git(src: R, rela_path: Path, index: State) -> Res!<ToGitOutcome<R>>
      lowers_to
        rust: "{{self}}.convert_to_git({{src}}, {{rela_path}}, {{index}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn convert_to_worktree(src: List<Int>, rela_path: BStr, options: Options) -> Res!<ToWorktreeOutcome>
      lowers_to
        rust: "{{self}}.convert_to_worktree({{src}}, {{rela_path}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn worktree_file_to_object(rela_path: BStr, index: State) -> Res!<Opt<(ObjectId, EntryKind, Metadata)>>
      lowers_to
        rust: "{{self}}.worktree_file_to_object({{rela_path}}, {{index}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn driver_context_mut() -> Context
      lowers_to
        rust: "{{self}}.driver_context_mut({{args}})"

  struct Buffer
    path repository::freelist

  struct ThreadSafeRepository
    path types
    fn to_thread_local() -> Repository
      lowers_to
        rust: "{{self}}.to_thread_local({{args}})"
    fn path() -> Path
      lowers_to
        rust: "{{self}}.path({{args}})"
    fn git_dir() -> Path
      lowers_to
        rust: "{{self}}.git_dir({{args}})"
    fn work_dir() -> Opt<Path>
      lowers_to
        rust: "{{self}}.work_dir({{args}})"
    fn objects_dir() -> Path
      lowers_to
        rust: "{{self}}.objects_dir({{args}})"
    fn open(path: PathBuf) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open_opts(path: PathBuf, options: Options) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::open_opts({{path}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open_with_environment_overrides(fallback_directory: PathBuf, trust_map: Mapping<Options>) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::open_with_environment_overrides({{fallback_directory}}, {{trust_map}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn init(directory: Path, kind: Kind, options: Options) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::init({{directory}}, {{kind}}, {{options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn init_opts(directory: Path, kind: Kind, create_options: Options, open_options: Options) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::init_opts({{directory}}, {{kind}}, {{create_options}}, {{open_options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn discover(directory: Path) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::discover({{directory}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn discover_opts(directory: Path, options: Options, trust_map: Mapping<Options>) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::discover_opts({{directory}}, {{options}}, {{trust_map}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn discover_with_environment_overrides(directory: Path) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::discover_with_environment_overrides({{directory}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn discover_with_environment_overrides_opts(directory: Path, options: Options, trust_map: Mapping<Options>) -> Res!<Self>
      lowers_to
        rust: "gix::ThreadSafeRepository::discover_with_environment_overrides_opts({{directory}}, {{options}}, {{trust_map}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Walk
    path revision::walk::iter_impl

  struct Info
    path revision::walk
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn object() -> Res!<Commit>
      lowers_to
        rust: "{{self}}.object({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn parent_ids() -> Unknown
      lowers_to
        rust: "{{self}}.parent_ids({{args}})"
    fn commit_time() -> SecondsSinceUnixEpoch
      lowers_to
        rust: "{{self}}.commit_time({{args}})"
    fn new(info: Info, repo: Repository) -> Self
      lowers_to
        rust: "gix::Info::new({{info}}, {{repo}})"
    fn detach() -> Info
      lowers_to
        rust: "{{self}}.detach({{args}})"

  struct Options
    path revision::spec::parse::types
    fn isolated() -> Self
      lowers_to
        rust: "gix::Options::isolated({{args}})"
    fn modify(f: Unknown)
      lowers_to
        rust: "{{self}}.modify({{f}})"
    fn config_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.config_overrides({{values}})"
    fn cli_overrides(values: Unknown) -> Self
      lowers_to
        rust: "{{self}}.cli_overrides({{values}})"
    fn object_store_slots(slots: Slots) -> Self
      lowers_to
        rust: "{{self}}.object_store_slots({{slots}})"
    fn permissions(permissions: Permissions) -> Self
      lowers_to
        rust: "{{self}}.permissions({{permissions}})"
    fn open_path_as_is(enable: Bool) -> Self
      lowers_to
        rust: "{{self}}.open_path_as_is({{enable}})"
    fn with(trust: Trust) -> Self
      lowers_to
        rust: "{{self}}.with({{trust}})"
    fn bail_if_untrusted(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.bail_if_untrusted({{toggle}})"
    fn filter_config_section(filter: Unknown) -> Self
      lowers_to
        rust: "{{self}}.filter_config_section({{filter}})"
    fn lossy_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.lossy_config({{toggle}})"
    fn strict_config(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.strict_config({{toggle}})"
    fn open(path: PathBuf) -> Res!<ThreadSafeRepository>
      lowers_to
        rust: "{{self}}.open({{path}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn no_locations() -> Self
      lowers_to
        rust: "{{self}}.no_locations({{args}})"
    fn track_filename() -> Self
      lowers_to
        rust: "{{self}}.track_filename({{args}})"
    fn track_path() -> Self
      lowers_to
        rust: "{{self}}.track_path({{args}})"
    fn track_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.track_rewrites({{renames}})"
    fn with_rewrites(renames: Opt<Rewrites>) -> Self
      lowers_to
        rust: "{{self}}.with_rewrites({{renames}})"
    fn empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.empty_patterns_match_prefix({{toggle}})"
    fn set_empty_patterns_match_prefix(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_empty_patterns_match_prefix({{toggle}})"
    fn recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.recurse_repositories({{toggle}})"
    fn set_recurse_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_recurse_repositories({{toggle}})"
    fn emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_pruned({{toggle}})"
    fn set_emit_pruned(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_pruned({{toggle}})"
    fn emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_ignored({{value}})"
    fn set_emit_ignored(value: Opt<EmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_ignored({{value}})"
    fn for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.for_deletion({{value}})"
    fn set_for_deletion(value: Opt<ForDeletionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_for_deletion({{value}})"
    fn emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_tracked({{toggle}})"
    fn set_emit_tracked(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_tracked({{toggle}})"
    fn emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.emit_untracked({{mode}})"
    fn set_emit_untracked(mode: EmissionMode) -> Self
      lowers_to
        rust: "{{self}}.set_emit_untracked({{mode}})"
    fn emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.emit_empty_directories({{toggle}})"
    fn set_emit_empty_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_emit_empty_directories({{toggle}})"
    fn classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.classify_untracked_bare_repositories({{toggle}})"
    fn set_classify_untracked_bare_repositories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_classify_untracked_bare_repositories({{toggle}})"
    fn emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.emit_collapsed({{value}})"
    fn set_emit_collapsed(value: Opt<CollapsedEntriesEmissionMode>) -> Self
      lowers_to
        rust: "{{self}}.set_emit_collapsed({{value}})"
    fn symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.symlinks_to_directories_are_ignored_like_directories({{toggle}})"
    fn set_symlinks_to_directories_are_ignored_like_directories(toggle: Bool) -> Self
      lowers_to
        rust: "{{self}}.set_symlinks_to_directories_are_ignored_like_directories({{toggle}})"

  struct Error
    path object::errors::write
    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ParseTime(Error)
    CommitterMissing
    AuthorMissing
    NewCommitAs(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    WriteObject(Error)
    FindCommit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    MakeAttributes(Box<Unknown>)
    Defaults(Error)
    ParseSpec(Error)
    NormalizeSpec(Error)
    RepoPrefix(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FilesystemConfig(Error)
    Defaults(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Find(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Head(Error)
    PeelToId(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Head(Error)
    PeelToCommit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    HeadCommit(Error)
    DecodeCommit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    HeadCommit(Error)
    CommitTree(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    SubmoduleStatus(Error)
    IgnoreConfig(Error)
    DiffSubmoduleIgnoreConfig(GenericErrorWithValue)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Find(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Url(Error)
    RewrittenUrlInvalid

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Submodule
    AsConfigured
    Given

    fn status(ignore: Ignore, check_dirty: Bool) -> Res!<Status>
      lowers_to
        rust: "{{self}}.status({{ignore}}, {{check_dirty}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn status_opts(ignore: Ignore, check_dirty: Bool, adjust_options: Unknown) -> Res!<Status>
      lowers_to
        rust: "{{self}}.status_opts({{ignore}}, {{check_dirty}}, {{adjust_options}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn name() -> BStr
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn validated_name() -> Res!<BStr>
      lowers_to
        rust: "{{self}}.validated_name({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn path() -> Res!<BString>
      lowers_to
        rust: "{{self}}.path({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn url() -> Res!<Url>
      lowers_to
        rust: "{{self}}.url({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn update() -> Res!<Opt<Update>>
      lowers_to
        rust: "{{self}}.update({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn branch() -> Res!<Opt<Branch>>
      lowers_to
        rust: "{{self}}.branch({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn fetch_recurse() -> Res!<Opt<FetchRecurse>>
      lowers_to
        rust: "{{self}}.fetch_recurse({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn ignore() -> Res!<Opt<Ignore>>
      lowers_to
        rust: "{{self}}.ignore({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn shallow() -> Res!<Opt<Bool>>
      lowers_to
        rust: "{{self}}.shallow({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn is_active() -> Res!<Bool>
      lowers_to
        rust: "{{self}}.is_active({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn index_id() -> Res!<Opt<ObjectId>>
      lowers_to
        rust: "{{self}}.index_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn head_id() -> Res!<Opt<ObjectId>>
      lowers_to
        rust: "{{self}}.head_id({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.git_dir({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn work_dir() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.work_dir({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn git_dir_try_old_form() -> Res!<PathBuf>
      lowers_to
        rust: "{{self}}.git_dir_try_old_form({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn state() -> Res!<State>
      lowers_to
        rust: "{{self}}.state({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn open() -> Res!<Opt<Repository>>
      lowers_to
        rust: "{{self}}.open({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  enum Error
    TagOpt(GenericErrorWithValue)
    RefSpec
    Url
    Init(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindExisting(Error)
    FindExistingReferences(Error)
    Init(Error)
    UrlParse(Error)
    ExactlyOneRemoteNotAvailable

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Item
    Modification
    DirectoryContents
    Rewrite

    fn summary() -> Opt<Summary>
      lowers_to
        rust: "{{self}}.summary({{args}})"
    fn rela_path() -> BStr
      lowers_to
        rust: "{{self}}.rela_path({{args}})"
    fn location() -> BStr
      lowers_to
        rust: "{{self}}.location({{args}})"

  enum RewriteSource
    RewriteFromIndex
    CopyFromDirectoryEntry

    fn rela_path() -> BStr
      lowers_to
        rust: "{{self}}.rela_path({{args}})"

  enum Error
    Parse(Error)
    RangedRev

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenCache(Error)
    FindMergeBase(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindMergeBase(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenCache(Error)
    MergeBase(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    MissingCommit
    NoMergeBase
    MergeBase(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenCache(Error)
    MergeBaseOctopus(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum InProgress
    ApplyMailbox
    ApplyMailboxRebase
    Bisect
    CherryPick
    CherryPickSequence
    Merge
    Rebase
    RebaseInteractive
    Revert
    RevertSequence

  enum Error
    Configuration(Error)
    Io(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenModulesFile(Error)
    OpenIndex(Error)
    FindExistingBlob(Error)
    FindHeadRef(Error)
    PeelHeadRef(Error)
    PeelObjectToCommit(Error)
    TreeFromCommit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindTree(Error)
    OpenTree(Error)
    AttributesCache(Error)
    FilterPipeline(Error)
    CommandContext(Error)
    NotATree

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    StatusPlatform(Error)
    CreateStatusIterator(Error)
    TreeIndexStatus(Error)
    HeadTreeId(Error)
    OpenWorktreeIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    MissingWorkDir
    AttributesAndExcludes(Error)
    Pathspec(Error)
    Prefix(Error)
    FilesystemOptions(Error)
    IndexAsWorktreeWithRenames(Error)
    StatOptions(Error)
    ResourceCache(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum TrackRenames
    AsConfigured
    Given(Rewrites)
    Disabled

  enum Error
    IndexFromMTree(Error)
    RewritesConfiguration(Error)
    DiffResourceCache(Error)
    TreeIndexDiff(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    DirwalkOptions(Error)
    ConfigureUntrackedFiles(GenericErrorWithValue)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Kind
    WithWorktree
    Bare

    fn attach(repo: Repository) -> Head
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  enum Error
    CurrentDir(Error)
    Init(Error)
    Open(Error)
    InvalidBranchName
    EditHeadForDefaultBranch(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Discover(Error)
    Open(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    NameMissing
    Span(Error)
    ConfigValue(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum AsError
    Save(Error)
    Name(Error)

  enum Error
    State(Error)
    HeadId(Error)
    IndexId(Error)
    OpenRepository(Error)
    IgnoreConfiguration(Error)
    StatusPlatform(Error)
    StatusIter(Error)
    NextStatusItem(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ModuleBoolean(Error)
    ConfigurationFallback(GenericErrorWithValue)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    InitIsActivePlatform(Error)
    QueryIsActive(Error)
    InitAttributes(Error)
    InitPathspecDefaults(Error)
    ObtainIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    PathConfiguration(Error)
    Index(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    HeadCommit(Error)
    CommitTree(Error)
    PeelTree(Error)
    PathConfiguration(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    PathConfiguration(Error)
    InvalidGitDirFileTarget
    GitDir(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    GitDirTryOldForm(Error)
    GitDir(Error)
    PathConfiguration(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenRepository(Error)
    GitDir(Error)
    PathConfiguration(Error)
    WorktreeDirInaccessible(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    BareRepository
    NoHeadTree(Error)
    IndexFromTree
    BooleanConfig(Error)
    WriteIndex(Error)
    CheckoutOptions(Error)
    IndexCheckout(Error)
    Peel(Error)
    OpenArcOdb(Error)
    FindHead(Error)
    PeelHeadToId(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum ProgressId
    CheckoutFiles
    BytesWritten

  enum Error
    Init(Error)
    CommitterOrFallback(Error)
    UrlParse(Error)
    CanonicalizeUrl

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenCache(Error)
    Describe(Error)
    ShortId(Error)
    RefIter(Error)
    RefIterInit(Error)
    DetermineIsDirty(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Io(Error)
    RelativePath(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum SelectRef
    AnnotatedTags
    AllTags
    AllRefs

  enum Change
    Addition
    Deletion
    Modification
    Rewrite

    fn diff(resource_cache: Platform) -> Res!<Platform>
      lowers_to
        rust: "{{self}}.diff({{resource_cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_change_ref(change: ChangeRef, old_repo: Repository, new_repo: Repository) -> Self
      lowers_to
        rust: "gix::Change::from_change_ref({{change}}, {{old_repo}}, {{new_repo}})"
    fn detach() -> ChangeDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn location() -> BStr
      lowers_to
        rust: "{{self}}.location({{args}})"
    fn entry_mode() -> EntryMode
      lowers_to
        rust: "{{self}}.entry_mode({{args}})"

  enum Error
    ConfigIndexThreads(GenericErrorWithValue)
    ConfigSkipHash(Error)
    IndexFile(Error)
    IndexCorrupt(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenIndex(Error)
    CreateCache(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    OpenIndex(Error)
    CreateCache(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Init(Error)
    OpenIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    WriteTree(Error)
    MissingObject
    InvalidFilename

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    MissingWorktree
    IO
    WriteBlob(Error)
    ConvertToGit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ProcessHunk(E)
    PrepareDiff(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Change
    Addition
    Deletion
    Modification

    fn diff(resource_cache: Platform) -> Res!<Platform>
      lowers_to
        rust: "{{self}}.diff({{resource_cache}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_change_ref(change: ChangeRef, old_repo: Repository, new_repo: Repository) -> Self
      lowers_to
        rust: "gix::Change::from_change_ref({{change}}, {{old_repo}}, {{new_repo}})"
    fn detach() -> ChangeDetached
      lowers_to
        rust: "{{self}}.detach({{args}})"
    fn id() -> Id
      lowers_to
        rust: "{{self}}.id({{args}})"
    fn location() -> BStr
      lowers_to
        rust: "{{self}}.location({{args}})"
    fn entry_mode() -> EntryMode
      lowers_to
        rust: "{{self}}.entry_mode({{args}})"

  enum Error
    Diff(Error)
    ForEach(Box<Unknown>)
    ResourceCache(Error)
    RenameTracking(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    CreateResourceCache(Error)
    ForEachChange(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Kind
    Symbolic(Reference)
    Unborn(FullName)
    Detached

    fn attach(repo: Repository) -> Head
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  enum Direction
    Push
    Fetch

    fn as_str() -> Str
      lowers_to
        rust: "{{self}}.as_str({{args}})"

  enum Error
    Find(Error)
    UrlParse(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Peel(Error)
    Unborn
    ObjectKind(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Peel(Error)
    Unborn

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindExistingObject(Error)
    PeelReference(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    PeelToObject(Error)
    ObjectKind(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Index(Error)
    SpawnThread(Error)
    ConfigSkipHash(Error)
    PrepareSubmodules(Error)
    HeadTreeIndex(Error)
    HeadTreeId(Error)
    AttributesAndExcludes(Error)
    Pathspec(Error)
    HeadTreeDiff(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    PackedObjectsCount(Error)
    DisambiguatePrefix(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Open(Error)
    FindExistingReference(Error)
    RemoteInit(Error)
    FindExistingRemote(Error)
    CredentialHelperConfig(Error)
    Other(E)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    CheckRoundTripEncodings(Error)
    SafeCrlf(GenericErrorWithValue)
    Driver
    CommandContext(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    DiffAlgorithm(Error)
    WorktreeFilterOptions(Error)
    DiffDrivers(Error)
    DiffPipelineOptions(Error)
    CommandContext(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindExistingObject(Error)
    NotFound

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Decode(Error)
    UnexpectedType

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Item
    IndexWorktree(Item)
    TreeIndex(Change)

    fn summary() -> Opt<Summary>
      lowers_to
        rust: "{{self}}.summary({{args}})"
    fn rela_path() -> BStr
      lowers_to
        rust: "{{self}}.rela_path({{args}})"
    fn location() -> BStr
      lowers_to
        rust: "{{self}}.location({{args}})"

  enum Error
    IndexWorktree(Error)
    TreeIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Default
    Nothing
    Current
    Upstream
    Simple
    Matching

  enum Error
    ConfigDiffRenames(GenericError)
    ConfigDiffRenameLimit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Tracking
    Disabled
    Renames
    RenamesAndCopies

  enum Error
    FilesystemCapabilities(Error)
    BigFileThreshold(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    CurrentDir(Error)
    IoOpen
    IoWrite
    DirectoryExists
    DirectoryNotEmpty
    CreateDirectory
    Span(Error)
    ConfigValue(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    DecodeTree(Error)
    ValidationOptions(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    RewritesConfiguration(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Open(Error)
    MissingWorktree
    MissingGitDirFile(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    InvalidUseHttpPath
    CoreAskpass(Error)
    BooleanConfig(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Boolean(Error)
    UnsignedInteger(Error)
    ConnectTimeout(Error)
    InvalidProxyAuthMethod
    ConfigureProxyAuthenticate(Error)
    InvalidSslVersion(Error)
    InvalidHttpVersion(GenericErrorWithValue)
    InvalidFollowRedirects(GenericErrorWithValue)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    InvalidKey
    SectionHeader(Error)
    Span(Error)
    ConfigValue(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindExistingObject(Error)
    Decode(Error)
    ParseDate(Error)
    ObjectKind

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Name
    Symbol(Cow<Str>)
    Url(Cow<BStr>)

    fn as_bstr() -> BStr
      lowers_to
        rust: "{{self}}.as_bstr({{args}})"
    fn as_symbol() -> Opt<Str>
      lowers_to
        rust: "{{self}}.as_symbol({{args}})"
    fn as_url() -> Opt<BStr>
      lowers_to
        rust: "{{self}}.as_url({{args}})"
    fn to_owned() -> Name
      lowers_to
        rust: "{{self}}.to_owned({{args}})"

  enum Error
    ValidateFetchRemoteRefName(Error)
    PushDefault(GenericErrorWithValue)
    FindPushRemote(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ValidateTrackingRef(Error)
    RemoteRef(Error)
    FindRemote(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    SymbolicReference
    ReferenceEdit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FileTransactionPrepare(Error)
    FileTransactionCommit(Error)
    NameValidation(Error)
    LockTimeoutConfiguration(Error)
    ParseCommitterTime(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ToId(Error)
    PackedRefsOpen(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FollowToObject(Error)
    PackedRefsOpen(Error)
    FindObject(Error)
    PeelObject(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FollowToObject(Error)
    PackedRefsOpen(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum SubSectionRequirement
    Never
    Parameter(Str)

  enum Link
    EnvironmentOverride(Str)
    FallbackKey(Unknown)

  enum Note
    Informative(Str)
    Deviation(Str)

  enum ObjectKindHint
    Commit
    Committish
    Tree
    Treeish
    Blob

  enum UntrackedFiles
    None
    Collapsed
    Files

  enum Error
    Validate(Error)
    Name

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    SetRaw(Error)
    Validate(Error)
    SubSectionRequired
    SubSectionForbidden

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigureAttributes(Error)
    ConfigureExcludes(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Io(Error)
    AttributesFileInterpolation(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Io(Error)
    EnvironmentPermission(Error<PathBuf>)
    ExcludesFilePathInterpolation(Error)
    ParsePreciousEnabled(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    CommitGraphIfEnabled(Error)
    DiffAlgorithm(Error)
    DiffResourceCache(Error)
    Blame(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    BigFileThreshold(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigBoolean(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigCheckStat(GenericErrorWithValue)
    ConfigBoolean(Error)
    CheckoutWorkers(Error)
    Attributes(Error)
    FilterPipelineOptions(Error)
    CommandContext(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    BranchCategory
    FindRemote(Error)
    AmbiguousRemotes
    ValidateUpstreamBranch(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigBoolean(Error)
    ConfigUnsigned(Error)
    ConfigTypedString(GenericErrorWithValue)
    ConfigCompression(GenericError)
    RefsNamespace(Error)
    UnsupportedObjectFormat
    ObjectFormatRequiresV1
    UnsupportedRepositoryFormatVersion
    CoreAbbrev(Error)
    Io
    Init(Error)
    ResolveIncludes(Error)
    Span(Error)
    ConfigValue(Error)
    FromEnv(Error)
    PathInterpolation
    ConfigOverrides

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigCheckStat(GenericErrorWithValue)
    ConfigBoolean(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Boolean(Error)
    ParseBool(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    SimpleTraversal(Error)
    ShallowCommits(Error)
    ConfigBoolean(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Unknown
    Unimplemented

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ResourceCache(Error)
    Index(Error)
    AttributeStack(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    DiffOptions(Error)
    CreateResourceCache(Error)
    TreeDiff(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    InvalidInteger
    ConfigValue
    InterpolatePath
    IllformedUtf8
    ParseUrl(Error)
    Http(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Walk(Error)
    MissingWorkDir
    Excludes(Error)
    Pathspec(Error)
    Prefix(Error)
    FilesystemOptions(Error)
    ListWorktrees(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum IndexPersistedOrInMemory
    Persisted(Index)
    InMemory(File)

    fn into_owned() -> File
      lowers_to
        rust: "{{self}}.into_owned({{args}})"

  enum OwnedOrStaticAtomicBool
    Owned
    Shared(AtomicBool)

  enum Error
    SpawnThread(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    HeadCommit(Error)
    DecodeCommit(Error)
    TreeTraverse(Error)
    BareAttributes(Error)
    WorktreeIndex(Error)
    Init(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ConfigBoolean(Error)
    OpenCommitGraph(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Time(Error)
    Span(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    HeadCommit(Error)
    TreeId(Error)
    TraverseTree(Error)
    OpenIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ReadHead(Error)
    FindCommit(Error)
    PeelToTree(Error)
    TreeId(Error)
    TraverseTree(Error)
    OpenIndex(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    IndexFromTree
    BooleanConfig(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    Config(Error)
    NotARepository
    Io(Error)
    UnsafeGitDir
    EnvironmentAccessDenied(Error<PathBuf>)
    PrefixNotRelative(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    SimpleTraversal(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Sorting
    BreadthFirst
    ByCommitTime(CommitTimeOrder)
    ByCommitTimeCutoff

  enum Error
    Realpath(Error)
    OutsideOfRepository
    AbsolutePathOutsideOfRepository

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum RefsHint
    PreferObjectOnFullLengthHexShaUseRefOtherwise
    PreferObject
    PreferRef
    Fail

  enum Kind
    Common
    Submodule
    LinkedWorkTree

    fn attach(repo: Repository) -> Head
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  enum Error
    Io(Error)
    BlobSpec(Error)
    PathInterpolate(Error)
    FindExisting(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    FindTree(Error)
    InitEditor(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum CandidateInfo
    FindError
    Object
    Tag
    Commit

  enum Error
    Find(Error)
    Convert(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    WorktreeCacheAtPath(Error)
    Convert(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    WorktreeCacheAtPath(Error)
    Convert(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ReferenceNameValidation(Error)
    WriteObject(Error)
    ReferenceEdit(Error)
    DateParseError(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  enum Error
    ParseTime(Error)
    CommitterMissing
    AuthorMissing
    ReferenceNameValidation(Error)
    WriteObject(Error)
    ReferenceEdit(Error)

    fn from_value(key: Unknown, value: BString) -> Self
      lowers_to
        rust: "gix::Error::from_value({{key}}, {{value}})"
    fn with_source(err: E) -> Self
      lowers_to
        rust: "{{self}}.with_source({{err}})"
    fn with_value(value: BString) -> Self
      lowers_to
        rust: "{{self}}.with_value({{value}})"
    fn is_corrupted() -> Bool
      lowers_to
        rust: "{{self}}.is_corrupted({{args}})"

  trait ObjectIdExt
    fn ancestors(find: Find) -> Simple<Find, Unknown>
      lowers_to
        rust: "{{self}}.ancestors({{find}})"
    fn attach(repo: Repository) -> Id
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  trait ReferenceExt
    fn attach(repo: Repository) -> Reference
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  trait RevSpecExt
    fn attach(repo: Repository) -> Spec
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  trait TreeIterExt
    fn traverse(state: StateMut, objects: Find, delegate: V) -> Res!<()>
      lowers_to
        rust: "{{self}}.traverse({{state}}, {{objects}}, {{delegate}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  trait TreeEntryRefExt
    fn attach(repo: Repository) -> EntryRef
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  trait TreeEntryExt
    fn attach(repo: Repository) -> Entry
      lowers_to
        rust: "{{self}}.attach({{repo}})"

  trait TreeDiffChangeExt
    fn attach(old_repo: Repository, new_repo: Repository) -> Change
      lowers_to
        rust: "{{self}}.attach({{old_repo}}, {{new_repo}})"

  trait ToComponents
    fn to_components() -> Unknown
      lowers_to
        rust: "{{self}}.to_components({{args}})"

  trait Section
    fn name() -> Str
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn keys() -> List<Unknown>
      lowers_to
        rust: "{{self}}.keys({{args}})"
    fn sub_sections() -> List<Unknown>
      lowers_to
        rust: "{{self}}.sub_sections({{args}})"
    fn parent() -> Opt<Unknown>
      lowers_to
        rust: "{{self}}.parent({{args}})"

  trait Validate
    fn validate(value: BStr) -> Res!<()>
      lowers_to
        rust: "{{self}}.validate({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  trait Key
    fn name() -> Str
      lowers_to
        rust: "{{self}}.name({{args}})"
    fn validate(value: BStr) -> Res!<()>
      lowers_to
        rust: "{{self}}.validate({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn section() -> Unknown
      lowers_to
        rust: "{{self}}.section({{args}})"
    fn subsection_requirement() -> Opt<SubSectionRequirement>
      lowers_to
        rust: "{{self}}.subsection_requirement({{args}})"
    fn link() -> Opt<Link>
      lowers_to
        rust: "{{self}}.link({{args}})"
    fn note() -> Opt<Note>
      lowers_to
        rust: "{{self}}.note({{args}})"
    fn environment_override() -> Opt<Str>
      lowers_to
        rust: "{{self}}.environment_override({{args}})"
    fn the_environment_override() -> Str
      lowers_to
        rust: "{{self}}.the_environment_override({{args}})"
    fn logical_name() -> Str
      lowers_to
        rust: "{{self}}.logical_name({{args}})"
    fn full_name(subsection: Opt<BStr>) -> Res!<BString>
      lowers_to
        rust: "{{self}}.full_name({{subsection}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn validated_assignment(value: BStr) -> Res!<BString>
      lowers_to
        rust: "{{self}}.validated_assignment({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn validated_assignment_fmt(value: Unknown) -> Res!<BString>
      lowers_to
        rust: "{{self}}.validated_assignment_fmt({{value}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn validated_assignment_with_subsection(value: BStr, subsection: BStr) -> Res!<BString>
      lowers_to
        rust: "{{self}}.validated_assignment_with_subsection({{value}}, {{subsection}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
