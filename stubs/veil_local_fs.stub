# Thin std::fs facade for VEIL-authored File adapters.
# Real crate: crates/veil_local_fs (workspace member).
# MISSION: filesystem domain stays out of veil-codegen; adapters use this stub.
stub veil_local_fs path:../crates/veil_local_fs

  struct LocalFs
    fn create_dir_all(path: Str) -> Res!
      lowers_to
        rust: "{{self}}.create_dir_all({{path}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn write(path: Str, data: Str) -> Res!
      lowers_to
        rust: "{{self}}.write({{path}}, {{data}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn read(path: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.read({{path}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    # Named path_exists so it is not confused with ObjectStorage.exists port method.
    fn path_exists(path: Str) -> Bool
      lowers_to
        rust: "{{self}}.path_exists({{path}})"
    fn path_is_file(path: Str) -> Bool
      lowers_to
        rust: "{{self}}.path_is_file({{path}})"
    fn list_dir(path: Str) -> Res!<List<Str>>
      lowers_to
        rust: "{{self}}.list_dir({{path}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn list_json_files(path: Str) -> Res!<List<Str>>
      lowers_to
        rust: "{{self}}.list_json_files({{path}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn join(a: Str, b: Str) -> Str
      lowers_to
        rust: "{{self}}.join({{a}}, {{b}})"
    # Deploy / storage helpers
    fn projects_dir() -> Str
      lowers_to
        rust: "{{self}}.projects_dir()"
    fn read_project_deploy(slug: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.read_project_deploy({{slug}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn read_toml_json(path: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.read_toml_json({{path}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    # Parse a TOML string and return it as JSON (for S3-sourced veil.toml content).
    fn parse_toml_str(content: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.parse_toml_str({{content}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    # Query [module.<namespace>] sections from a set of TOML entries.
    # entries_json: JSON array of {repo_id, repo_name, slug, branch, raw: bytes}.
    # Returns JSON: {results: [...], count: N}
    fn query_modules_from_tomls(entries_json: Str, module: Str, filters_json: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.query_modules_from_tomls({{entries_json}}, {{module}}, {{filters_json}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn project_unit_names(slug: Str) -> Res!<List<Str>>
      lowers_to
        rust: "{{self}}.project_unit_names({{slug}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
    fn project_unit_type(slug: Str, name: Str) -> Res!<Str>
      lowers_to
        rust: "{{self}}.project_unit_type({{slug}}, {{name}}).map_err(|e| {{error_model.external}}(format!(\"{e:?}\")))?"""
