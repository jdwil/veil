stub reqwest 0.13.4
  # @generated veil-stub-gen 1
  # source crates.io
  # surface full
  # cargo_name reqwest
  # generated_at 2026-08-20T19:48:59Z
  # rustdoc_fingerprint 982f8a1c3953d1c4
  # Auto-inferred codegen policy from rustdoc (do not hand-edit; re-run veil stub-gen)
  root_types Client
  async_methods send, text, json, bytes, execute
  harness_field Client """
{
    reqwest::Client::builder()
        .connect_timeout(std::time::Duration::from_secs(5))
        .timeout(std::time::Duration::from_secs(30))
        .redirect(reqwest::redirect::Policy::none())
        .build()
        .expect("reqwest client")
}
"""
  fn for_host(host: S) -> Builder
    lowers_to
      rust: "reqwest::for_host({{host}})"
  fn never() -> Builder
    lowers_to
      rust: "reqwest::never({{args}})"

  struct Body
    path async_impl::body
    fn as_bytes() -> Opt<List<Int>>
      lowers_to
        rust: "{{self}}.as_bytes({{args}})"
    fn wrap(inner: B) -> Body
      lowers_to
        rust: "reqwest::Body::wrap({{inner}})"

  struct Response
    path async_impl::response
    fn status() -> StatusCode
      lowers_to
        rust: "{{self}}.status({{args}})"
    fn version() -> Version
      lowers_to
        rust: "{{self}}.version({{args}})"
    fn headers() -> HeaderMap
      lowers_to
        rust: "{{self}}.headers({{args}})"
    fn headers_mut() -> HeaderMap
      lowers_to
        rust: "{{self}}.headers_mut({{args}})"
    fn content_length() -> Opt<Int>
      lowers_to
        rust: "{{self}}.content_length({{args}})"
    fn url() -> Url
      lowers_to
        rust: "{{self}}.url({{args}})"
    fn remote_addr() -> Opt<SocketAddr>
      lowers_to
        rust: "{{self}}.remote_addr({{args}})"
    fn extensions() -> Extensions
      lowers_to
        rust: "{{self}}.extensions({{args}})"
    fn extensions_mut() -> Extensions
      lowers_to
        rust: "{{self}}.extensions_mut({{args}})"
    fn text() -> BoxFuture<Res!<Str>>
      lowers_to
        rust: "{{self}}.text({{args}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn text_with_charset(default_encoding: Str) -> BoxFuture<Res!<Str>>
      lowers_to
        rust: "{{self}}.text_with_charset({{default_encoding}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn bytes() -> BoxFuture<Res!<Bytes>>
      lowers_to
        rust: "{{self}}.bytes({{args}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn chunk() -> BoxFuture<Res!<Opt<Bytes>>>
      lowers_to
        rust: "{{self}}.chunk({{args}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn error_for_status() -> Res!<Self>
      lowers_to
        rust: "{{self}}.error_for_status({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn error_for_status_ref() -> Res!<Self>
      lowers_to
        rust: "{{self}}.error_for_status_ref({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn upgrade() -> BoxFuture<Res!<Upgraded>>
      lowers_to
        rust: "{{self}}.upgrade({{args}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct ClientBuilder
    path async_impl::client
    fn new() -> Self
      lowers_to
        rust: "reqwest::ClientBuilder::new({{args}})"
    fn build() -> Res!<Client>
      lowers_to
        rust: "{{self}}.build({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn user_agent(value: V) -> ClientBuilder
      lowers_to
        rust: "{{self}}.user_agent({{value}})"
    fn default_headers(headers: HeaderMap) -> ClientBuilder
      lowers_to
        rust: "{{self}}.default_headers({{headers}})"
    fn no_gzip() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_gzip({{args}})"
    fn no_brotli() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_brotli({{args}})"
    fn no_zstd() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_zstd({{args}})"
    fn no_deflate() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_deflate({{args}})"
    fn redirect(policy: Policy) -> ClientBuilder
      lowers_to
        rust: "{{self}}.redirect({{policy}})"
    fn referer(enable: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.referer({{enable}})"
    fn retry(policy: Builder) -> ClientBuilder
      lowers_to
        rust: "{{self}}.retry({{policy}})"
    fn proxy(proxy: Proxy) -> ClientBuilder
      lowers_to
        rust: "{{self}}.proxy({{proxy}})"
    fn no_proxy() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_proxy({{args}})"
    fn timeout(timeout: Duration) -> ClientBuilder
      lowers_to
        rust: "{{self}}.timeout({{timeout}})"
    fn read_timeout(timeout: Duration) -> ClientBuilder
      lowers_to
        rust: "{{self}}.read_timeout({{timeout}})"
    fn connect_timeout(timeout: Duration) -> ClientBuilder
      lowers_to
        rust: "{{self}}.connect_timeout({{timeout}})"
    fn connection_verbose(verbose: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.connection_verbose({{verbose}})"
    fn pool_idle_timeout(val: D) -> ClientBuilder
      lowers_to
        rust: "{{self}}.pool_idle_timeout({{val}})"
    fn pool_max_idle_per_host(max: Int) -> ClientBuilder
      lowers_to
        rust: "{{self}}.pool_max_idle_per_host({{max}})"
    fn http1_title_case_headers() -> ClientBuilder
      lowers_to
        rust: "{{self}}.http1_title_case_headers({{args}})"
    fn http1_allow_obsolete_multiline_headers_in_responses(value: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http1_allow_obsolete_multiline_headers_in_responses({{value}})"
    fn http1_ignore_invalid_headers_in_responses(value: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http1_ignore_invalid_headers_in_responses({{value}})"
    fn http1_allow_spaces_after_header_name_in_responses(value: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http1_allow_spaces_after_header_name_in_responses({{value}})"
    fn http1_only() -> ClientBuilder
      lowers_to
        rust: "{{self}}.http1_only({{args}})"
    fn http09_responses() -> ClientBuilder
      lowers_to
        rust: "{{self}}.http09_responses({{args}})"
    fn http2_prior_knowledge() -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_prior_knowledge({{args}})"
    fn http2_initial_stream_window_size(sz: Opt<Int>) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_initial_stream_window_size({{sz}})"
    fn http2_initial_connection_window_size(sz: Opt<Int>) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_initial_connection_window_size({{sz}})"
    fn http2_adaptive_window(enabled: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_adaptive_window({{enabled}})"
    fn http2_max_frame_size(sz: Opt<Int>) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_max_frame_size({{sz}})"
    fn http2_max_header_list_size(max_header_size_bytes: Int) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_max_header_list_size({{max_header_size_bytes}})"
    fn http2_keep_alive_interval(interval: Opt<Duration>) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_keep_alive_interval({{interval}})"
    fn http2_keep_alive_timeout(timeout: Duration) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_keep_alive_timeout({{timeout}})"
    fn http2_keep_alive_while_idle(enabled: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.http2_keep_alive_while_idle({{enabled}})"
    fn tcp_nodelay(enabled: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tcp_nodelay({{enabled}})"
    fn local_address(addr: T) -> ClientBuilder
      lowers_to
        rust: "{{self}}.local_address({{addr}})"
    fn interface(interface: Str) -> ClientBuilder
      lowers_to
        rust: "{{self}}.interface({{interface}})"
    fn tcp_keepalive(val: D) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tcp_keepalive({{val}})"
    fn tcp_keepalive_interval(val: D) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tcp_keepalive_interval({{val}})"
    fn tcp_keepalive_retries(retries: C) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tcp_keepalive_retries({{retries}})"
    fn tcp_user_timeout(val: D) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tcp_user_timeout({{val}})"
    fn unix_socket(path: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.unix_socket({{path}})"
    fn tls_certs_merge(certs: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_certs_merge({{certs}})"
    fn tls_certs_only(certs: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_certs_only({{certs}})"
    fn add_root_certificate(cert: Certificate) -> ClientBuilder
      lowers_to
        rust: "{{self}}.add_root_certificate({{cert}})"
    fn tls_crls_only(crls: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_crls_only({{crls}})"
    fn add_crl(crl: CertificateRevocationList) -> ClientBuilder
      lowers_to
        rust: "{{self}}.add_crl({{crl}})"
    fn add_crls(crls: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.add_crls({{crls}})"
    fn identity(identity: Identity) -> ClientBuilder
      lowers_to
        rust: "{{self}}.identity({{identity}})"
    fn tls_danger_accept_invalid_hostnames(accept_invalid_hostname: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_danger_accept_invalid_hostnames({{accept_invalid_hostname}})"
    fn danger_accept_invalid_hostnames(accept_invalid_hostname: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.danger_accept_invalid_hostnames({{accept_invalid_hostname}})"
    fn tls_danger_accept_invalid_certs(accept_invalid_certs: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_danger_accept_invalid_certs({{accept_invalid_certs}})"
    fn danger_accept_invalid_certs(accept_invalid_certs: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.danger_accept_invalid_certs({{accept_invalid_certs}})"
    fn tls_sni(tls_sni: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_sni({{tls_sni}})"
    fn tls_sslkeylogfile(on: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_sslkeylogfile({{on}})"
    fn tls_version_min(version: Version) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_version_min({{version}})"
    fn min_tls_version(version: Version) -> ClientBuilder
      lowers_to
        rust: "{{self}}.min_tls_version({{version}})"
    fn tls_version_max(version: Version) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_version_max({{version}})"
    fn max_tls_version(version: Version) -> ClientBuilder
      lowers_to
        rust: "{{self}}.max_tls_version({{version}})"
    fn tls_backend_rustls() -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_backend_rustls({{args}})"
    fn use_rustls_tls() -> ClientBuilder
      lowers_to
        rust: "{{self}}.use_rustls_tls({{args}})"
    fn tls_backend_preconfigured(tls: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_backend_preconfigured({{tls}})"
    fn use_preconfigured_tls(tls: Unknown) -> ClientBuilder
      lowers_to
        rust: "{{self}}.use_preconfigured_tls({{tls}})"
    fn tls_info(tls_info: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.tls_info({{tls_info}})"
    fn https_only(enabled: Bool) -> ClientBuilder
      lowers_to
        rust: "{{self}}.https_only({{enabled}})"
    fn no_hickory_dns() -> ClientBuilder
      lowers_to
        rust: "{{self}}.no_hickory_dns({{args}})"
    fn resolve(domain: Str, addr: SocketAddr) -> ClientBuilder
      lowers_to
        rust: "{{self}}.resolve({{domain}}, {{addr}})"
    fn resolve_to_addrs(domain: Str, addrs: List<SocketAddr>) -> ClientBuilder
      lowers_to
        rust: "{{self}}.resolve_to_addrs({{domain}}, {{addrs}})"
    fn dns_resolver(resolver: R) -> ClientBuilder
      lowers_to
        rust: "{{self}}.dns_resolver({{resolver}})"
    fn connector_layer(layer: L) -> ClientBuilder
      lowers_to
        rust: "{{self}}.connector_layer({{layer}})"

  struct Client
    path async_impl::client
    fn new() -> Client
      lowers_to
        rust: "reqwest::Client::new({{args}})"
    fn builder() -> ClientBuilder
      lowers_to
        rust: "reqwest::Client::builder({{args}})"
    fn get(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.get({{url}})"
    fn post(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.post({{url}})"
    fn put(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.put({{url}})"
    fn patch(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.patch({{url}})"
    fn delete(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.delete({{url}})"
    fn head(url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.head({{url}})"
    fn request(method: Method, url: U) -> RequestBuilder
      lowers_to
        rust: "{{self}}.request({{method}}, {{url}})"
    fn execute(request: Request) -> BoxFuture<Res!<Response>>
      lowers_to
        rust: "{{self}}.execute({{request}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct RequestBuilder
    path async_impl::request
    fn from_parts(client: Client, request: Request) -> RequestBuilder
      lowers_to
        rust: "reqwest::RequestBuilder::from_parts({{client}}, {{request}})"
    fn header(key: K, value: V) -> RequestBuilder
      lowers_to
        rust: "{{self}}.header({{key}}, {{value}})"
    fn headers(headers: HeaderMap) -> RequestBuilder
      lowers_to
        rust: "{{self}}.headers({{headers}})"
    fn basic_auth(username: U, password: Opt<P>) -> RequestBuilder
      lowers_to
        rust: "{{self}}.basic_auth({{username}}, {{password}})"
    fn bearer_auth(token: T) -> RequestBuilder
      lowers_to
        rust: "{{self}}.bearer_auth({{token}})"
    fn body(body: T) -> RequestBuilder
      lowers_to
        rust: "{{self}}.body({{body}})"
    fn timeout(timeout: Duration) -> RequestBuilder
      lowers_to
        rust: "{{self}}.timeout({{timeout}})"
    fn version(version: Version) -> RequestBuilder
      lowers_to
        rust: "{{self}}.version({{version}})"
    fn build() -> Res!<Request>
      lowers_to
        rust: "{{self}}.build({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn build_split() -> (Client, Res!<Request>)
      lowers_to
        rust: "{{self}}.build_split({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn send() -> BoxFuture<Res!<Response>>
      lowers_to
        rust: "{{self}}.send({{args}}).await.map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn try_clone() -> Opt<RequestBuilder>
      lowers_to
        rust: "{{self}}.try_clone({{args}})"

  struct Request
    path async_impl::request
    fn new(method: Method, url: Url) -> Self
      lowers_to
        rust: "reqwest::Request::new({{method}}, {{url}})"
    fn method() -> Method
      lowers_to
        rust: "{{self}}.method({{args}})"
    fn method_mut() -> Method
      lowers_to
        rust: "{{self}}.method_mut({{args}})"
    fn url() -> Url
      lowers_to
        rust: "{{self}}.url({{args}})"
    fn url_mut() -> Url
      lowers_to
        rust: "{{self}}.url_mut({{args}})"
    fn headers() -> HeaderMap
      lowers_to
        rust: "{{self}}.headers({{args}})"
    fn headers_mut() -> HeaderMap
      lowers_to
        rust: "{{self}}.headers_mut({{args}})"
    fn body() -> Opt<Body>
      lowers_to
        rust: "{{self}}.body({{args}})"
    fn body_mut() -> Opt<Body>
      lowers_to
        rust: "{{self}}.body_mut({{args}})"
    fn timeout() -> Opt<Duration>
      lowers_to
        rust: "{{self}}.timeout({{args}})"
    fn timeout_mut() -> Opt<Duration>
      lowers_to
        rust: "{{self}}.timeout_mut({{args}})"
    fn version() -> Version
      lowers_to
        rust: "{{self}}.version({{args}})"
    fn version_mut() -> Version
      lowers_to
        rust: "{{self}}.version_mut({{args}})"
    fn try_clone() -> Opt<Request>
      lowers_to
        rust: "{{self}}.try_clone({{args}})"

  struct Error
    path error
    fn url() -> Opt<Url>
      lowers_to
        rust: "{{self}}.url({{args}})"
    fn url_mut() -> Opt<Url>
      lowers_to
        rust: "{{self}}.url_mut({{args}})"
    fn with_url(url: Url) -> Self
      lowers_to
        rust: "{{self}}.with_url({{url}})"
    fn without_url() -> Self
      lowers_to
        rust: "{{self}}.without_url({{args}})"
    fn is_builder() -> Bool
      lowers_to
        rust: "{{self}}.is_builder({{args}})"
    fn is_redirect() -> Bool
      lowers_to
        rust: "{{self}}.is_redirect({{args}})"
    fn is_status() -> Bool
      lowers_to
        rust: "{{self}}.is_status({{args}})"
    fn is_timeout() -> Bool
      lowers_to
        rust: "{{self}}.is_timeout({{args}})"
    fn is_request() -> Bool
      lowers_to
        rust: "{{self}}.is_request({{args}})"
    fn is_connect() -> Bool
      lowers_to
        rust: "{{self}}.is_connect({{args}})"
    fn is_body() -> Bool
      lowers_to
        rust: "{{self}}.is_body({{args}})"
    fn is_decode() -> Bool
      lowers_to
        rust: "{{self}}.is_decode({{args}})"
    fn status() -> Opt<StatusCode>
      lowers_to
        rust: "{{self}}.status({{args}})"
    fn is_upgrade() -> Bool
      lowers_to
        rust: "{{self}}.is_upgrade({{args}})"

  struct Pending
    path async_impl::client

  struct Policy
    path redirect
    fn limited(max: Int) -> Self
      lowers_to
        rust: "reqwest::Policy::limited({{max}})"
    fn none() -> Self
      lowers_to
        rust: "reqwest::Policy::none({{args}})"
    fn custom(policy: T) -> Self
      lowers_to
        rust: "reqwest::Policy::custom({{policy}})"
    fn redirect(attempt: Attempt) -> Action
      lowers_to
        rust: "{{self}}.redirect({{attempt}})"

  struct Builder
    path retry
    fn scoped(scope: Unknown) -> Self
      lowers_to
        rust: "reqwest::Builder::scoped({{scope}})"
    fn no_budget() -> Self
      lowers_to
        rust: "{{self}}.no_budget({{args}})"
    fn max_extra_load(extra_percent: F64) -> Self
      lowers_to
        rust: "{{self}}.max_extra_load({{extra_percent}})"
    fn max_retries_per_request(max: Int) -> Self
      lowers_to
        rust: "{{self}}.max_retries_per_request({{max}})"
    fn classify_fn(func: F) -> Self
      lowers_to
        rust: "{{self}}.classify_fn({{func}})"
    fn classify(classifier: Unknown) -> Self
      lowers_to
        rust: "{{self}}.classify({{classifier}})"

  struct Proxy
    path proxy
    fn http(proxy_scheme: U) -> Res!<Proxy>
      lowers_to
        rust: "reqwest::Proxy::http({{proxy_scheme}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn https(proxy_scheme: U) -> Res!<Proxy>
      lowers_to
        rust: "reqwest::Proxy::https({{proxy_scheme}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn all(proxy_scheme: U) -> Res!<Proxy>
      lowers_to
        rust: "reqwest::Proxy::all({{proxy_scheme}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn custom(fun: F) -> Proxy
      lowers_to
        rust: "reqwest::Proxy::custom({{fun}})"
    fn basic_auth(username: Str, password: Str) -> Proxy
      lowers_to
        rust: "{{self}}.basic_auth({{username}}, {{password}})"
    fn custom_http_auth(header_value: HeaderValue) -> Proxy
      lowers_to
        rust: "{{self}}.custom_http_auth({{header_value}})"
    fn headers(headers: HeaderMap) -> Proxy
      lowers_to
        rust: "{{self}}.headers({{headers}})"
    fn no_proxy(no_proxy: Opt<NoProxy>) -> Proxy
      lowers_to
        rust: "{{self}}.no_proxy({{no_proxy}})"

  struct Certificate
    path tls
    fn from_der(der: List<Int>) -> Res!<Certificate>
      lowers_to
        rust: "reqwest::Certificate::from_der({{der}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_pem(pem: List<Int>) -> Res!<Certificate>
      lowers_to
        rust: "reqwest::Certificate::from_pem({{pem}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_pem_bundle(pem_bundle: List<Int>) -> Res!<List<Certificate>>
      lowers_to
        rust: "reqwest::Certificate::from_pem_bundle({{pem_bundle}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct CertificateRevocationList
    path tls
    fn from_pem(pem: List<Int>) -> Res!<CertificateRevocationList>
      lowers_to
        rust: "reqwest::CertificateRevocationList::from_pem({{pem}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn from_pem_bundle(pem_bundle: List<Int>) -> Res!<List<CertificateRevocationList>>
      lowers_to
        rust: "reqwest::CertificateRevocationList::from_pem_bundle({{pem_bundle}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Identity
    path tls
    fn from_pem(buf: List<Int>) -> Res!<Identity>
      lowers_to
        rust: "reqwest::Identity::from_pem({{buf}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  struct Version
    path tls

  struct Unnameable
    path connect::sealed

  struct Conn
    path connect::sealed

  struct Upgraded
    path async_impl::upgrade

  struct Internal
    path connect::uds

  struct InvalidNameError
    path dns::resolve::sealed

  struct Name
    path dns::resolve
    fn as_str() -> Str
      lowers_to
        rust: "{{self}}.as_str({{args}})"

  struct NoProxy
    path proxy
    fn from_env() -> Opt<NoProxy>
      lowers_to
        rust: "reqwest::NoProxy::from_env({{args}})"
    fn from_string(no_proxy_list: Str) -> Opt<Self>
      lowers_to
        rust: "reqwest::NoProxy::from_string({{no_proxy_list}})"

  struct Attempt
    path redirect
    fn status() -> StatusCode
      lowers_to
        rust: "{{self}}.status({{args}})"
    fn url() -> Url
      lowers_to
        rust: "{{self}}.url({{args}})"
    fn previous() -> List<Url>
      lowers_to
        rust: "{{self}}.previous({{args}})"
    fn follow() -> Action
      lowers_to
        rust: "{{self}}.follow({{args}})"
    fn stop() -> Action
      lowers_to
        rust: "{{self}}.stop({{args}})"
    fn error(error: E) -> Action
      lowers_to
        rust: "{{self}}.error({{error}})"

  struct Action
    path redirect

  struct ReqRep
    path retry::classify

  struct TlsInfo
    path tls
    fn peer_certificate() -> Opt<List<Int>>
      lowers_to
        rust: "{{self}}.peer_certificate({{args}})"

  enum Action
    Success
    Retryable

  trait ResponseBuilderExt
    fn url(url: Url) -> Self
      lowers_to
        rust: "{{self}}.url({{url}})"

  trait Resolve
    fn resolve(name: Name) -> Resolving
      lowers_to
        rust: "{{self}}.resolve({{name}})"

  trait IntoProxy
    fn into_proxy() -> Res!<Url>
      lowers_to
        rust: "{{self}}.into_proxy({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"

  trait Scope
    fn applies_to(req: Request<Body>) -> Bool
      lowers_to
        rust: "{{self}}.applies_to({{req}})"

  trait Classify
    fn classify(req_rep: ReqRep) -> Action
      lowers_to
        rust: "{{self}}.classify({{req_rep}})"

  trait IntoUrlSealed
    fn into_url() -> Res!<Url>
      lowers_to
        rust: "{{self}}.into_url({{args}}).map_err(|e| {{error_model.external}}(format!("{e:?}")))?"
    fn as_str() -> Str
      lowers_to
        rust: "{{self}}.as_str({{args}})"
