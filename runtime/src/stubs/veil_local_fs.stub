# Thin std::fs facade for VEIL-authored File adapters.
# Real crate: runtime/local_fs (path dep in generated workspace — not in engine).
# MISSION: filesystem domain stays out of veil-codegen; adapters use this stub.
# Version `path:…` → workspace path dep (relative to gen output root).
stub veil_local_fs path:../local_fs

  struct LocalFs
    fn create_dir_all(path: Str) -> Res!
    fn write(path: Str, data: Str) -> Res!
    fn read(path: Str) -> Res!<Str>
    # Named path_exists so it is not confused with ObjectStorage.exists port method.
    fn path_exists(path: Str) -> Bool
    fn path_is_file(path: Str) -> Bool
    fn list_dir(path: Str) -> Res!<List<Str>>
    fn list_json_files(path: Str) -> Res!<List<Str>>
    fn join(a: Str, b: Str) -> Str
    # Deploy / storage helpers
    fn projects_dir() -> Str
    fn read_project_deploy(slug: Str) -> Res!<Str>
    fn read_toml_json(path: Str) -> Res!<Str>
    # Parse a TOML string and return it as JSON (for S3-sourced veil.toml content).
    fn parse_toml_str(content: Str) -> Res!<Str>
    # Query [module.<namespace>] sections from a set of TOML entries.
    # entries_json: JSON array of {repo_id, repo_name, slug, branch, raw: bytes}.
    # Returns JSON: {results: [...], count: N}
    fn query_modules_from_tomls(entries_json: Str, module: Str, filters_json: Str) -> Res!<Str>
    fn project_unit_names(slug: Str) -> Res!<List<Str>>
    fn project_unit_type(slug: Str, name: Str) -> Res!<Str>
